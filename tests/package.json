{
  "name": "prueba-map-japan-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.6.2",
    "postgres": "^3.4.9"
  }
}
