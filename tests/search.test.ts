import { describe, it, expect } from 'vitest';
// import { searchLocation } from '../src/modules/search';

describe('Search Module', () => {
  it('placeholder test', () => {
    expect(true).toBe(true);
  });
});