# Candidate Requirements: v3.2 Security & Code Health Hardening

**Status:** Draft — not yet a committed milestone. Formalize via `/gsd-new-milestone` once v3.1 ships (ROADMAP.md still shows v3.1 in progress as of 2026-07-22).

**Source:** Synthesizes `ANALISIS-REPO.md` (Fable 5, 2026-07-22, static read of the current tree) and `codex-review.md` (Codex, 2026-06-23, live-environment run — **stale**: predates all of Phase 17–19's login-harness rewrite, so its E2E-reliability findings are superseded, not open work). Overlapping findings deduped to one entry citing both. Each item below carries a **verification note** — either spot-checked against current `main` on 2026-07-22, or marked `needs-triage` (not re-checked; carry the source doc's claim forward as unverified).

---

## Cluster 0 — Already addressed by v3.1 (do not re-plan)

codex-review.md's headline risk was "the E2E harness doesn't reliably prove auth/session/passkey behavior." That's exactly what v3.1 Phases 15–19 did:

- `loginViaKcForm` shared helper (Phase 17) replaces the four independent, fragile KC-navigation implementations codex's finding #4 flagged.
- `sessionStorage`-token replay assumption (codex #4, #12, #18; ANALISIS B-08) — the underlying test *behavior* was fixed in Phase 17; the stale doc comment (ANALISIS B-08, codex #18) is still open, see BUG-06 below.
- Firefox/WebKit instability (codex #5) — directly addressed by Phase 19's D-03 storageState parity fix and this session's session-test@local dedicated-user fix for cross-contamination.
- `session-management.spec.ts` selector/flow fragility (codex #12) — Phase 19 (SESSION-01), closing now.
- WebAuthn required-action race (codex implicit in #1 test failures) — Phase 17/18/19 all hardened around this (`hitRequiredAction`, Case B guards).

**Do not** re-open these as v3.2 work. If Phase 19's closure doc shows a clean full-suite run, this cluster is done.

---

## Cluster 1 — Security (Priority 0)

| ID | Finding | Source | Verified 2026-07-22 |
|----|---------|--------|----------------------|
| SEC-01 | OTP code generated with `Math.random()`, not a CSPRNG | ANALISIS S-01, codex #6 | **OPEN** — `backend/src/routes/auth.ts:123` unchanged |
| SEC-02 | News/events widget interpolates unescaped RSS fields into `innerHTML` (XSS) | ANALISIS S-02, codex #7 | **OPEN** — `frontend/src/modules/widgets.ts` still has 5 raw `innerHTML` sinks |
| SEC-03 | Widget content sourced from untrusted third-party CORS proxies (allorigins/corsproxy) | ANALISIS S-03, codex #7 | needs-triage (same file as SEC-02; same fix window) |
| SEC-04 | No frontend CSP (GitHub Pages can't set headers; no `<meta http-equiv>` fallback) | ANALISIS M-07, codex #16 | needs-triage |
| SEC-05 | JWKS cache force-invalidated by any bad-signature request → DoS amplification against Keycloak | ANALISIS S-04 | needs-triage |
| SEC-06 | JWT verification errors leak issuer URL/realm/audience to client | ANALISIS S-05 | needs-triage |
| SEC-07 | OTP attempt-counter TOCTOU (non-atomic increment) | ANALISIS S-06, codex #6 | needs-triage |
| SEC-08 | Email fails open to local Mailpit if `RESEND_API_KEY` missing in prod | ANALISIS S-07 | needs-triage |
| SEC-09 | `passkeys/profile.ts` renders `userLabel` (user-controlled) via unescaped `innerHTML` | ANALISIS S-15, codex #7 | needs-triage |
| SEC-10 | `SearchBar.highlightMatch` builds `<mark>` HTML by unescaped substring concat | ANALISIS S-17, codex #7 | needs-triage — latent today (static data), becomes live once search indexes API data |
| SEC-11 | Keycloak `error.ftl` dropped `kcSanitize()` before `?no_esc` (inconsistent with `login.ftl`) | ANALISIS S-13 | needs-triage |
| SEC-12 | WebAuthn passwordless `ALTERNATIVE` execution is silently ignored by Keycloak (`REQUIRED`+`ALTERNATIVE` mixed at same level) | codex #1 | **PARTIALLY ADDRESSED** — `terraform/keycloak/flows.tf:34-39` now has an explicit `SECURITY-NOTE` documenting this is known and non-bypassing; the actual flow restructure (single REQUIRED subflow with webauthn/password as ALTERNATIVES inside it) is still undone |
| SEC-13 | Backend/Terraform config sources diverge (`main.tf` vs `realm-export.json` vs `apply-local-settings.sh` disagree on `browserFlow`) | codex #2 | needs-triage |

## Cluster 2 — Reliability bugs & deploy safety (Priority 0/1)

| ID | Finding | Source | Verified 2026-07-22 |
|----|---------|--------|----------------------|
| INFRA-01 | `deploy-frontend.yml`/`deploy-backend.yml` trigger on push-to-main by path with **no `needs`/`workflow_run` gate on `ci.yml`** — broken code can deploy to prod | ANALISIS M-08 (implied), codex #8/O-01 | **CONFIRMED OPEN** — both workflows' `on.push` block has no CI dependency |
| INFRA-02 | Backend fails `wrangler deploy --dry-run` — `pg`/node-postgres bundled into the Worker pulls in Node builtins | codex #8/O-02 | needs-triage (not re-run; `compatibility_date` still `2024-01-01` per INFRA-03, consistent with codex's finding still being live) |
| INFRA-03 | `backend/wrangler.toml` `compatibility_date` frozen at `2024-01-01` despite `nodejs_compat` | ANALISIS O-04 | **CONFIRMED OPEN** — unchanged |
| INFRA-04 | `wrangler deploy` unpinned (`npx wrangler`, picks up latest each run) | ANALISIS O-03 | needs-triage |
| BUG-01 | Activity drag-reorder: optimistic array swap doesn't update `order_index`, so the very next re-render (which sorts by `order_index`) undoes the visible reorder; the API's returned updated rows are also discarded | ANALISIS B-14 | **CONFIRMED OPEN** — `frontend/src/pages/trip-edit/activities.ts:407-439`, `reorderActivities()` return value discarded at line 429 |
| BUG-02 | `request()` in API client returns a promise that never resolves on 401 — callers hang forever (disabled buttons, dead spinners) | ANALISIS B-01, codex (implicit) | needs-triage |
| BUG-03 | First-login race: parallel `getUserByKeycloakId → null` calls both `createUser`, second hits unique-index violation → intermittent 500 | ANALISIS B-02, S-14 | needs-triage — codex flags `users.ts` `/me` as the likely trigger point (no `DATABASE_URL`/try-catch guard either) |
| BUG-04 | `getHotel()` JSDoc says "null if none" but backend 404s → client throws instead of returning null | ANALISIS B-03 | needs-triage |
| BUG-05 | `reorderActivities` backend doesn't validate `orderedIds` covers the full set for the day | ANALISIS B-16 | needs-triage |
| BUG-06 | Stale comment: `keycloak.ts:32` still says tokens live in `sessionStorage`; keycloak-js actually keeps them in memory | ANALISIS B-08, codex #18 (also flags `docs/security/rfc9700-checklist.md`) | **CONFIRMED OPEN** — one-line comment fix, plus the checklist doc if still stale |

## Cluster 3 — Dependencies & secrets (Priority 0/1)

| ID | Finding | Source | Verified 2026-07-22 |
|----|---------|--------|----------------------|
| DEP-01 | `npm audit --workspaces` reports high/critical runtime vulnerabilities (`hono`, `drizzle-orm`, `dompurify`, `undici`, `ws`) | ANALISIS (implied via S-*), codex #9 | **CONFIRMED OPEN AND WORSE** — re-run today: **21 vulnerabilities (2 critical, 10 high, 9 moderate)**, up from codex's 18 on 2026-06-23. `npm audit fix --force` pulls in `wrangler@4.x` (breaking) |
| DEP-02 | Gitleaks full-history scan found 14 `generic-api-key` findings (all in `.planning/` docs and test fixtures); none confirmed as live secrets by TruffleHog verified-scan, but untriaged | codex #21 | needs-triage — re-run Gitleaks before declaring clean; do not assume codex's 2026-06-23 result still holds after ~40 more commits |
| DEP-03 | No Gitleaks/TruffleHog/axe/Lighthouse in CI — regressions on any of the above are invisible until manually re-run | codex O-05/#19 backlog | needs-triage |

## Cluster 4 — Accessibility (Priority 1)

All from codex #19 (2026-06-23 axe-core 4.12.1 + Lighthouse 13.4.0 run against `dist`) — **not re-verified this session**, carry forward as needs-triage:

- A11Y-01: `aria-expanded` on a plain `<input>` (invalid ARIA role/attribute combo) — repeated on 12 of 13 pages, the single highest-leverage a11y fix
- A11Y-02: Contrast violations — landing (`#landing-loading > span`, `.demo-countdown-title`), dashboard `.nav-link`, profile (13 nodes)
- A11Y-03: `trip.html` has zero `<h1>` in its initial (loading/guest/error) state
- A11Y-04: `tokyo.html` — `heading-order` and `target-size` violations
- A11Y-05: Mobile LCP — landing 5.856s, Tokyo 6.261s (Lighthouse mobile-throttled)

## Cluster 5 — Lower priority / architecture debt (Priority 2)

Carried forward without re-verification — small, low-risk, "improve when touching the file" items:

- ARCH-01: `createDb` returns `any` (ANALISIS N-01) — cascades into `trips.ts:132` cast-to-`any` (BUG, ANALISIS B-07)
- ARCH-02: Dual DB driver selected by `localhost` substring match on connection string, not an explicit env var (ANALISIS N-02)
- ARCH-03: `trips.ts` (1017 lines, all ownership-cascade authorization logic) has **zero unit tests** — highest security-ROI test gap in the repo (ANALISIS T-01)
- ARCH-04: `upsertUser` exists but is dead code; real login path never refreshes email/name from Keycloak (ANALISIS B-12)
- M-01: Extract `DATABASE_URL`/`getDb` middleware — ~20 duplicated guard blocks across `trips.ts`/`auth.ts` (ANALISIS M-01)
- M-02: `resolveActivity` does 4 sequential SELECTs (trip→dest→day→activity) instead of one JOIN (ANALISIS M-02)
- M-03: Zod schemas don't validate `start_date <= end_date`, lat/lng range, or reject empty-object PATCH bodies (ANALISIS M-05)
- PWA-01: Manifest icons are remote (`cdn-icons-png.flaticon.com`) instead of first-party/precached (ANALISIS M-08 implied, codex #13)
- PROD-01: codex found `manud.github.io/PruebaMapJapan` returning 404 and the Workers API hostname not resolving on 2026-06-23 — **needs-triage, re-check before treating as current state**; may simply reflect that this repo doesn't have production deployed yet (matches STATE.md's existing `DEPLOY` deferred item)

---

## Proposed phase breakdown (draft — to be finalized by `/gsd-plan-phase` when this milestone starts)

1. **Phase 20 — Critical security fixes**: SEC-01, SEC-02, SEC-03, SEC-04, SEC-09, SEC-10 (the XSS/OTP-RNG package — same shape as ANALISIS's own "package 2" recommendation, reusing the `DOMPurify` pattern already proven correct in `tripDetail.ts`/`map.ts`)
2. **Phase 21 — Deploy & build safety**: INFRA-01, INFRA-02, INFRA-03, INFRA-04, DEP-01 (gate deploys on CI, fix the Worker build, upgrade runtime deps behind regression tests)
3. **Phase 22 — Reliability bugs**: BUG-01 (highest user-visible impact), BUG-02, BUG-03, BUG-04, BUG-05, BUG-06
4. **Phase 23 — Secrets & accessibility**: DEP-02, DEP-03, A11Y-01..05
5. **Phase 24 — Architecture debt & test coverage** (stretch, lowest priority): ARCH-01..04, M-01..03, PWA-01

Not included in phase breakdown: PROD-01 (production deployment) — already tracked as a deferred item in STATE.md's Deferred Items table; don't duplicate.
