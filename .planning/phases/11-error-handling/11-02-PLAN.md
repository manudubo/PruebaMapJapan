---
phase: 11-error-handling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/index.ts
  - backend/src/types/index.ts
autonomous: true
requirements:
  - ERR-04

must_haves:
  truths:
    - "app.onError() returns JSON with a 'code' field set to 'internal_error'"
    - "ApiResponse<T> type includes an optional 'code' field"
    - "backend typecheck is clean"
  artifacts:
    - path: "backend/src/index.ts"
      provides: "onError handler with typed code field"
      contains: "code: 'internal_error'"
    - path: "backend/src/types/index.ts"
      provides: "ApiResponse<T> interface with optional code field"
      contains: "code?: string"
  key_links:
    - from: "backend/src/index.ts app.onError()"
      to: "backend/src/types/index.ts ApiResponse"
      via: "c.json() return shape"
      pattern: "code: 'internal_error'"
---

<objective>
Update backend error handling to include typed error codes in the onError response and add the code field to ApiResponse<T>.

Purpose: The frontend ApiError class (Plan 11-03) reads `envelope.code` from API responses. This plan establishes the backend side of that contract. Backend is fully independent of toast.ts — it runs in Wave 1 in parallel with Plan 11-01.

Output: backend/src/index.ts onError emits `{ success: false, error: '...', code: 'internal_error' }`; backend/src/types/index.ts ApiResponse<T> has optional `code?: string`.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/manud/Documents/GitHub/PruebaMapJapan/.planning/PROJECT.md
@C:/Users/manud/Documents/GitHub/PruebaMapJapan/.planning/ROADMAP.md
@C:/Users/manud/Documents/GitHub/PruebaMapJapan/.planning/phases/11-error-handling/11-CONTEXT.md
@C:/Users/manud/Documents/GitHub\PruebaMapJapan/.planning/phases/11-error-handling/11-PATTERNS.md

<interfaces>
<!-- Current backend types — executor must read these before modifying -->

From backend/src/types/index.ts (current ApiResponse — no code field yet):
```typescript
export interface ApiResponse<T = unknown> {
  success: boolean;
  data?: T;
  error?: string;
  message?: string;
}
```

From backend/src/index.ts lines 40-43 (current onError — no code field):
```typescript
app.onError((err, c) => {
  console.error('Unhandled error:', err);
  return c.json({ success: false, error: 'Internal server error' }, 500);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add code field to ApiResponse and update onError</name>
  <files>backend/src/types/index.ts, backend/src/index.ts</files>
  <read_first>
    - backend/src/types/index.ts (full file — see current ApiResponse shape; confirm no code field)
    - backend/src/index.ts (full file — find onError handler at ~line 40; confirm current shape)
    - backend/.planning/phases/11-error-handling/11-PATTERNS.md lines 415-446 (backend pattern block)
  </read_first>
  <action>
**Scope boundary (critical — do not exceed):** `app.onError()` fires ONLY for thrown exceptions that bubble out of route handlers. The 20+ inline `c.json({success:false,...})` returns in trips.ts and auth.ts route files are returned responses — they bypass `onError` entirely. Do NOT retrofit those inline returns. Only modify the two files listed.

**Step 1: `backend/src/types/index.ts`**

Find the `ApiResponse<T>` interface and add `code?: string` after the `error?` field:

```typescript
export interface ApiResponse<T = unknown> {
  success: boolean;
  data?: T;
  error?: string;
  code?: string;      // typed error code for client consumption (Phase 11)
  message?: string;
}
```

**Step 2: `backend/src/index.ts`**

Find the `app.onError()` handler (currently ~line 40) and add `code: 'internal_error'` to the response object:

```typescript
app.onError((err, c) => {
  console.error('Unhandled error:', err);
  return c.json({ success: false, error: 'Internal server error', code: 'internal_error' }, 500);
});
```

No other changes to index.ts. No imports added or removed.

After both edits, run `cd backend && npm run typecheck` to confirm 0 errors.
  </action>
  <verify>
    <automated>cd backend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/types/index.ts contains `code?: string` inside the ApiResponse interface
    - backend/src/index.ts contains `code: 'internal_error'` inside the onError handler
    - backend/src/index.ts does NOT contain new imports (no new import lines added)
    - `cd backend && npm run typecheck` exits 0
    - backend/src/index.ts line count is within 2 lines of original (only onError line changed)
  </acceptance_criteria>
  <done>ApiResponse has optional code field; onError returns code: 'internal_error'; backend typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend onError → HTTP response | Error details cross from server to client in JSON body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Information Disclosure | app.onError() response body | mitigate | `error` field is hardcoded string "Internal server error" — no stack trace, no err.message forwarded to client. `code` is a fixed enum value 'internal_error'. |
| T-11-02-02 | Tampering | ApiResponse code field in route returns | accept | Inline route `c.json()` returns (trips.ts, auth.ts) bypass onError and do not carry code — frontend shows generic messages regardless (D-07). No new attack surface. |
</threat_model>

<verification>
After plan completion:
1. `cd backend && npm run typecheck` — 0 errors
2. `rg "code\?: string" backend/src/types/index.ts` — returns match inside ApiResponse
3. `rg "code: 'internal_error'" backend/src/index.ts` — returns match inside onError
4. `rg "innerHTML" backend/src/` — returns nothing (N/A — backend has no DOM)
</verification>

<success_criteria>
- ApiResponse<T> has code?: string field
- onError emits code: 'internal_error'
- backend typecheck clean (0 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/11-error-handling/11-02-SUMMARY.md`
</output>
