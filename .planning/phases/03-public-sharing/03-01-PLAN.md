---
phase: 03-public-sharing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/public.test.ts
autonomous: true
requirements:
  - SHARE-02
  - SHARE-04

must_haves:
  truths:
    - "Slug-based public route tests exist and fail RED before 03-02 runs"
    - "Tests cover: valid slug + public trip, valid slug + private trip, invalid UUID format, valid UUID + no match"
  artifacts:
    - path: "backend/src/routes/public.test.ts"
      provides: "Failing tests for slug-based GET /api/public/trips/:slug"
      exports: []
  key_links:
    - from: "backend/src/routes/public.test.ts"
      to: "backend/src/index.ts"
      via: "app.request('/api/public/trips/:slug', {}, mockEnv)"
      pattern: "app\\.request.*public/trips"
---

<objective>
Write failing (RED) tests for the slug-based public trip route.

Purpose: Drive 03-02 backend implementation through TDD. These tests must fail before 03-02 ships and pass after.
Output: `backend/src/routes/public.test.ts` with 4 tests, at least Tests 3 and 4 RED.
</objective>

<execution_context>
@/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From backend/src/index.test.ts — established test pattern for this app -->

```typescript
import { describe, it, expect } from 'vitest';
import app from './index';
import type { Env } from './types';

const mockEnv: Env = {
  DATABASE_URL: 'postgresql://mock:mock@localhost/mockdb',
  KEYCLOAK_URL: 'http://localhost:8080',
  KEYCLOAK_REALM: 'japan-trip',
};

// Route hit via: await app.request('/api/public/trips/99999', {}, mockEnv)
// Without a real DB: route returns 404 or 500
// Pattern: expect([404, 500]).toContain(res.status)
```

<!-- From backend/src/routes/public.ts — current (integer-based) route being replaced -->

```typescript
// Current: GET /api/public/trips/:tripId  (integer param)
// After 03-02: GET /api/public/trips/:slug  (UUID string param)
// Valid UUID format: /^[0-9a-f-]{36}$/
// Invalid format → 400 "Invalid slug"
// Valid UUID, no match (or is_public=false) → 404
```
</interfaces>
</context>

<feature>
  <name>Slug-based public trip route — TDD RED</name>
  <files>backend/src/routes/public.test.ts</files>
  <behavior>
    The file does not yet exist. Create it with a describe block
    "Public trip route — slug-based (SHARE-02, SHARE-04)" containing 4 tests.

    Import pattern matches backend/src/index.test.ts exactly:
    - import app from '../index' (relative path from routes/ sub-dir)
    - import type { Env } from '../types'
    - mockEnv constant identical to index.test.ts

    Test 1 — Valid UUID, public trip → 200 or 500 (no real DB in unit tests):
      slug = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890'
      GET /api/public/trips/${slug}
      expect([200, 500]).toContain(res.status)  — same pragmatic pattern as index.test.ts:52-53
      body.success must be checked: if 200, expect(body.success).toBe(true); if 500, skip data check

    Test 2 — Valid UUID format, private trip (is_public=false) → 404 or 500:
      slug = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890'
      Same slug as Test 1 — the privacy check is enforced by DB logic, not testable without real DB.
      expect([404, 500]).toContain(res.status)
      This test documents the invariant; the real guard is the is_public=true filter in 03-02.
      Add a comment: "// Real privacy enforcement tested via is_public=true filter in getTripBySlug"

    Test 3 — Invalid UUID format → 400 (no DB hit needed — validation is synchronous):
      slug = 'not-a-uuid'
      GET /api/public/trips/not-a-uuid
      expect(res.status).toBe(400)                  ← WILL FAIL RED (current route does isNaN, not UUID regex)
      const body = await res.json()
      expect(body.success).toBe(false)
      expect(body.error).toBe('Invalid slug')        ← WILL FAIL RED (current error message is 'Invalid trip id')

    Test 4 — Valid UUID, no matching trip → 404 or 500:
      slug = '00000000-0000-0000-0000-000000000000' (all-zeros UUID, guaranteed no match)
      GET /api/public/trips/${slug}
      expect([404, 500]).toContain(res.status)       ← WILL FAIL RED on 400 because current route does isNaN('00000000...') = false
      const body = await res.json()
      if (res.status === 404) { expect(body.success).toBe(false) }

    RED confirmation: After writing tests, run `npm test` in backend/.
    Test 3 will fail because the route returns 400 with 'Invalid trip id' (not 'Invalid slug').
    Test 4 will fail because the current integer route parses '00000000-...' via Number() → NaN → 400 instead of 404/500.
    Tests 1 and 2 use [200/500] and [404/500] ranges to avoid DB dependency — they pass today (500) but
    document the expected behavior (200, 404) for after 03-02.
  </behavior>
  <implementation>
    Write only the test file. No production code changes.
    File path: backend/src/routes/public.test.ts
    Use relative imports (../index, ../types) since this file is inside src/routes/.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED tests for slug-based public route</name>
  <files>backend/src/routes/public.test.ts</files>
  <action>
    Create backend/src/routes/public.test.ts. File does not exist yet.

    Structure:
    ```
    import { describe, it, expect } from 'vitest';
    import app from '../index';
    import type { Env } from '../types';

    const mockEnv: Env = { ... };  // identical to index.test.ts mockEnv

    describe('Public trip route — slug-based (SHARE-02, SHARE-04)', () => {
      const VALID_SLUG = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890';

      it('valid UUID + public trip → 200 (or 500 without real DB)', async () => { ... });
      it('valid UUID + private trip → 404 (enforced by is_public filter)', async () => { ... });
      it('invalid UUID format → 400 "Invalid slug"', async () => { ... });
      it('valid UUID + no matching trip → 404 (or 500 without real DB)', async () => { ... });
    });
    ```

    See <feature><behavior> block above for per-test assertions.

    After writing: run `npm test` from backend/ to confirm RED state.
    Tests 3 and 4 must fail (current route uses integer param, returns 'Invalid trip id', not 'Invalid slug').
    Tests 1 and 2 use range assertions and will not fail today — they document the GREEN target.
  </action>
  <verify>
    <automated>cd C:\Users\manud\Documents\GitHub\PruebaMapJapan\backend && npm test -- --reporter=verbose 2>&1 | Select-String -Pattern "public.test|Invalid slug|FAIL|PASS"</automated>
  </verify>
  <done>
    File backend/src/routes/public.test.ts exists.
    `npm test` output shows at minimum Test 3 ("invalid UUID format → 400 'Invalid slug'") FAILING — confirming RED state.
    Test 4 ("no matching trip → 404") also FAILING.
    Tests 1 and 2 pass with [200/500] and [404/500] range checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | Unit tests call Hono app in-process via app.request(); no real network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | Test assertions | accept | Tests use [200,500] range — no real trip data exposed in test suite |
</threat_model>

<verification>
After 03-01 completes:
- `backend/src/routes/public.test.ts` exists
- `npm test` run from backend/ shows Test 3 and Test 4 in FAILED state
- No production files modified
</verification>

<success_criteria>
`public.test.ts` has 4 tests; at minimum Tests 3 and 4 are RED (failing).
The 03-02 executor will make these go GREEN by implementing the slug-based route.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-sharing/03-01-SUMMARY.md`
</output>
