---
phase: 05-internationalization-translate-all-user-facing-ui-strings-ht
plan: 06
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/pages/dashboard.ts
autonomous: true
requirements: [I18N-TS, I18N-LOCALE]
must_haves:
  truths:
    - "Dashboard trip cards show 'Public' badge (not 'Público')"
    - "Dashboard shows 'destination(s)' count (not Spanish 'destino(s)')"
    - "Dashboard shows 'Hello, {name}' greeting (not 'Hola')"
    - "Dashboard empty state reads English (not 'Todavía no tenés ningún viaje guardado.')"
    - "Dashboard edit links show 'Edit' (not 'Editar')"
    - "Dashboard date formatting uses en-US locale (English month names)"
  artifacts:
    - path: "frontend/src/pages/dashboard.ts"
      provides: "Translated dashboard page module with en-US dates"
  key_links:
    - from: "dashboard.ts:26"
      to: "trip card date display"
      via: "toLocaleDateString('es-ES')"
      pattern: "es-ES"
---

<objective>
Translate all Spanish strings in dashboard.ts including the es-ES locale call, aria-labels, badge text, plural destination count, empty state messages, greeting, and error strings.

Purpose: dashboard.ts drives all dynamic text on the trips list page. The es-ES locale call (line 26) produces Spanish month names at runtime, invisible to accent-char grep — must be changed explicitly.
Output: dashboard.ts with English strings and en-US locale throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate dashboard.ts</name>
  <files>frontend/src/pages/dashboard.ts</files>
  <read_first>
    - frontend/src/pages/dashboard.ts (read full file before editing)
    - 05-PATTERNS.md "Wave 4: frontend/src/pages/dashboard.ts" section (lines 458–499)
  </read_first>
  <action>
Apply the following exact string replacements in frontend/src/pages/dashboard.ts:

Line 26 (locale call in formatDateRange()):
  toLocaleDateString('es-ES', { day: 'numeric', month: 'short', year: 'numeric' })
  → toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' })

Line 37 (aria-label via setAttribute):
  `Ver viaje: ${trip.name}`
  → `View trip: ${trip.name}`

Line 51 (badge textContent):
  badge.textContent = 'Público'
  → badge.textContent = 'Public'

Line 81 (destinations count template literal):
  `${destCount} destino${destCount !== 1 ? 's' : ''}`
  → `${destCount} destination${destCount !== 1 ? 's' : ''}`

Line 94 (edit link text):
  editLink.textContent = 'Editar'
  → editLink.textContent = 'Edit'

Line 111 (empty state first paragraph):
  p1.textContent = 'Todavía no tenés ningún viaje guardado.'
  → p1.textContent = "You don't have any trips saved yet."

Line 112 (empty state second paragraph):
  p2.textContent = '¡Creá tu primer itinerario con el botón de arriba!'
  → p2.textContent = 'Create your first itinerary with the button above!'

Line 127 (greeting template literal):
  `Hola, ${name.split(' ')[0]}`
  → `Hello, ${name.split(' ')[0]}`

Line 127 (greeting fallback):
  'Mis viajes'
  → 'My Trips'

Line 245 (fetch error):
  `No se pudieron cargar los viajes: ${(err as Error).message}`
  → `Could not load trips: ${(err as Error).message}`

After applying all replacements, run the verification commands to confirm zero residuals.
  </action>
  <verify>
    <automated>rg "[áéíóúñ¿¡]" frontend/src/pages/dashboard.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg "[áéíóúñ¿¡]" frontend/src/pages/dashboard.ts returns zero matches
    - rg "es-ES" frontend/src/pages/dashboard.ts returns zero matches
  </acceptance_criteria>
  <done>dashboard.ts has English strings throughout and en-US locale for date formatting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | String-only TS edits — no auth or security changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | dashboard.ts UI strings | accept | Text-only changes; trip data and routing logic unchanged |
</threat_model>

<verification>
- `rg "[áéíóúñ¿¡]" frontend/src/pages/dashboard.ts` returns zero
- `rg "es-ES" frontend/src/pages/dashboard.ts` returns zero
</verification>

<success_criteria>
- dashboard.ts: zero Spanish accent characters
- Locale call changed from 'es-ES' to 'en-US'
- All 10 string replacements applied exactly as specified
</success_criteria>

<output>
After completion, create `.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-06-SUMMARY.md`
</output>
