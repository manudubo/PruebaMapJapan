---
phase: 05-internationalization-translate-all-user-facing-ui-strings-ht
plan: 12
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10, 05-11]
files_modified: []
autonomous: true
requirements: [I18N-HTML, I18N-TS, I18N-LOCALE, I18N-PASSKEY, I18N-ACCENT]
must_haves:
  truths:
    - "No Spanish accent characters remain anywhere in frontend/src, frontend/*.html, or frontend/public/manifest.json"
    - "No es-ES locale calls remain in frontend/src"
    - "No lang='es' attributes remain in any HTML file or manifest.json"
    - "TypeScript compiler reports zero errors"
    - "Full Playwright test suite passes"
    - "Shared strings in map.ts and tripDetail.ts are identical English"
  artifacts:
    - path: "frontend/src"
      provides: "Fully translated TypeScript source directory"
    - path: "frontend/*.html"
      provides: "Fully translated HTML pages"
    - path: "frontend/public/manifest.json"
      provides: "Translated PWA manifest"
  key_links:
    - from: "rg '[áéíóúñ¿¡]'"
      to: "zero matches"
      via: "accent-char audit"
      pattern: "[áéíóúñ¿¡]"
---

<objective>
Run the complete validation suite to confirm all translations are correct and complete. This plan is a read-only verification pass — no file edits. Any failures indicate missed strings from a prior plan.

Purpose: Mechanical completeness verification. After 11 translation plans spanning 25+ files, a final full-tree audit catches any missed strings before the phase is marked complete.
Output: Green audit (accent audit + locale audit + lang audit + typecheck + Playwright).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-RESEARCH.md
@.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full accent-char and locale audit</name>
  <files></files>
  <read_first>
    - No file reads required — this task runs audit commands only
  </read_first>
  <action>
Run the following audit commands in sequence. For each command, note the output.
If any command returns matches, identify the file(s) and plan(s) responsible, fix the missed strings, then re-run.

AUDIT 1 — Full accent-char scan (must return zero):
  rg "[áéíóúñ¿¡]" frontend/src frontend/*.html frontend/public/manifest.json --files-with-matches

  NOTE: itinerary.ts IS in scope — it must return clean. No allowlist exemptions.
  NOTE: test fill data in tests/e2e/trip-edit.spec.ts and tests/e2e/trip-edit-integration.spec.ts
  contains `country: 'Japón'` as page.fill() INPUT DATA (not UI assertions) — this is intentional
  and must NOT be translated. If the scan returns these files, inspect whether the hit is in a
  toContain()/toHaveText() assertion (translate) or a page.fill() call (leave as-is).

AUDIT 2 — Locale call scan (must return zero):
  rg "es-ES" frontend/src

AUDIT 3 — HTML lang attribute scan (must return zero):
  rg 'lang="es"' frontend/*.html frontend/public/manifest.json

AUDIT 4 — Shared-string consistency check (map.ts vs tripDetail.ts):
  rg "Ver en Maps|View on Maps|Cómo llegar|Alojamiento|Filtrar por día" frontend/src/modules/map.ts frontend/src/pages/tripDetail.ts
  Expected: only English terms (View on Maps, Directions, Accommodation, Filter by day) appear; Spanish terms return zero.

If all 4 audits return zero (or only the allowed test fill data exception), proceed to Task 2.
If any audit fails, fix the missed strings in the relevant source file before proceeding.
  </action>
  <verify>
    <automated>rg "[áéíóúñ¿¡]" frontend/src frontend/*.html frontend/public/manifest.json --files-with-matches</automated>
  </verify>
  <acceptance_criteria>
    - Accent-char audit returns zero file matches (no files with Spanish characters)
    - rg "es-ES" frontend/src returns zero matches
    - rg 'lang="es"' frontend/*.html frontend/public/manifest.json returns zero matches
    - Shared-string consistency: Spanish terms absent from map.ts and tripDetail.ts
  </acceptance_criteria>
  <done>All 4 static audits return zero — no Spanish strings remain anywhere in the translated files</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript typecheck and Playwright full suite</name>
  <files></files>
  <read_first>
    - No file reads required — this task runs build and test commands only
  </read_first>
  <action>
Run the following verification commands:

STEP 1 — TypeScript typecheck (must pass with zero errors):
  npx tsc --noEmit

  If this fails: read the error output, identify which file has a type error, and fix it.
  Common cause: a string comparison that was changed in metadata.ts (line 54 comparison value).

STEP 2 — Playwright full test suite (must pass):
  npx playwright test

  If this fails:
  - First check uat-passkeys.spec.ts separately: npx playwright test uat-passkeys.spec.ts
  - If uat-passkeys fails: verify profile.ts line 78 translation and uat-passkeys.spec.ts line 108 assertion match
  - For any other test failure: identify whether the test asserts a UI string that was translated
    (update the assertion to English) or tests a non-string behavior (investigate separately)
  - Do NOT modify test fill data (country: 'Japón' in page.fill() calls) — these are not UI assertions
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0 (zero type errors)
    - npx playwright test passes (all specs green)
  </acceptance_criteria>
  <done>TypeScript compiles cleanly and all Playwright tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Validation-only plan — no file edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | Translation completeness | accept | Audit commands verify no Spanish strings remain; any miss is a cosmetic defect, not a security risk |
</threat_model>

<verification>
Final gate — ALL must pass before phase is marked complete:

1. `rg "[áéíóúñ¿¡]" frontend/src frontend/*.html frontend/public/manifest.json --files-with-matches` → zero files
2. `rg "es-ES" frontend/src` → zero matches
3. `rg 'lang="es"' frontend/*.html frontend/public/manifest.json` → zero matches
4. `npx tsc --noEmit` → exit code 0
5. `npx playwright test` → all specs pass
</verification>

<success_criteria>
- Zero Spanish accent characters in any source file under scope
- Zero es-ES locale calls
- Zero lang="es" HTML attributes
- TypeScript compiles without errors
- Full Playwright suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-12-SUMMARY.md`
</output>
