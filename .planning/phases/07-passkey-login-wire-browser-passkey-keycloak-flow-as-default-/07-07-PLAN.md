---
phase: 07-backend-hardening-kc-config
plan: "07"
type: execute
wave: 2
depends_on:
  - "07-03"
autonomous: false
requirements:
  - BACK-04
  - KC-01
  - KC-02
  - KC-03

must_haves:
  truths:
    - "terraform apply succeeds with no errors"
    - "password-forms subflow is visible in the browser-passkey flow in KC admin console"
    - "browser_flow is set to browser-passkey in KC realm"
    - "VERIFY_EMAIL required action is enabled in KC realm"
    - "japan-trip-worker client exists in KC realm with service accounts enabled"
    - "webauthn-register-passwordless required action exists (no duplicate, terraform plan shows no changes)"
  artifacts:
    - path: "terraform/keycloak/terraform.tfstate"
      provides: "Local Terraform state after apply"
      contains: "japan-trip-worker"
  key_links:
    - from: "terraform apply"
      to: "Keycloak realm japan-trip"
      via: "mrparkers/keycloak provider"
      pattern: "Apply complete!"
---

<objective>
Apply the Terraform Keycloak changes from Plan 07-03 (password-forms subflow + worker client + VERIFY_EMAIL + browserFlow flip). Then verify KC-03 is already satisfied (no duplicate required action). This is the only plan that actually modifies the live local Keycloak instance.

Purpose: All prior plans (07-01 through 07-06) modify source files but do not touch the running KC container. This plan is the gate that applies the IaC changes and confirms the KC state matches the HCL declarations.
Output: Terraform state updated; KC realm reflects all Phase 7 Terraform changes.
</objective>

<execution_context>
@C:/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-passkey-login-wire-browser-passkey-keycloak-flow-as-default-/07-CONTEXT.md
@.planning/phases/07-passkey-login-wire-browser-passkey-keycloak-flow-as-default-/07-RESEARCH.md
@.planning/phases/07-passkey-login-wire-browser-passkey-keycloak-flow-as-default-/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run terraform apply and verify KC state</name>
  <files>terraform/keycloak/terraform.tfstate</files>
  <read_first>
    - terraform/keycloak/main.tf (verify all changes from Plan 07-03 are present before applying)
    - terraform/keycloak/flows.tf (verify password-forms subflow is declared)
  </read_first>
  <action>
Prerequisite: Keycloak must be running. Verify first:
```bash
docker compose -f keycloak/docker-compose.yml ps keycloak
```
If KC is not running, start it:
```bash
docker compose -f keycloak/docker-compose.yml up -d keycloak
```
Wait for KC to be healthy (healthcheck is configured in docker-compose.yml from Phase 6).

Then run terraform plan to preview changes:
```bash
terraform -chdir=terraform/keycloak plan -var-file=local.tfvars
```
Review the plan output. Expected additions:
- `keycloak_authentication_subflow.password_forms` (new)
- `keycloak_authentication_execution.username_password_form` (new)
- `keycloak_openid_client.japan_trip_worker` (new)
- `data.keycloak_openid_client.realm_management` (data source read)
- `keycloak_openid_client_service_account_role.worker_manage_users` (new)
- `keycloak_required_action.verify_email` (new)
- Realm update: `browser_flow` "browser" → "browser-passkey", `access_code_lifespan_user_action` "5m" → "20m"

NOT in plan: `keycloak_required_action.webauthn_register_passwordless` — this already exists in state, no change expected.

If plan looks correct, apply:
```bash
terraform -chdir=terraform/keycloak apply -var-file=local.tfvars
```

Confirm with `yes` when prompted.
  </action>
  <verify>
    <automated>terraform -chdir=terraform/keycloak plan -var-file=local.tfvars -detailed-exitcode</automated>
  </verify>
  <acceptance_criteria>
    - `terraform -chdir=terraform/keycloak apply -var-file=local.tfvars` exits 0 with "Apply complete!"
    - `rg "japan-trip-worker" terraform/keycloak/terraform.tfstate` exits 0 (state has worker client)
    - `rg "VERIFY_EMAIL" terraform/keycloak/terraform.tfstate` exits 0 (state has VERIFY_EMAIL action)
    - `rg "browser-passkey" terraform/keycloak/terraform.tfstate` exits 0 (realm browser_flow updated)
    - `rg "password-forms" terraform/keycloak/terraform.tfstate` exits 0 (password-forms subflow in state)
  </acceptance_criteria>
  <done>terraform apply succeeds; KC realm state matches HCL declarations; no duplicate required actions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Terraform applied all Phase 7 KC changes to the local Keycloak instance:
    - password-forms ALTERNATIVE branch added to browser-passkey flow
    - browser_flow switched to "browser-passkey"  
    - japan-trip-worker client created with service account
    - VERIFY_EMAIL required action enabled (default action)
    - access_code_lifespan_user_action set to 20m
    KC-03: webauthn-register-passwordless Required Action still exists (was already there)
  </what-built>
  <how-to-verify>
    1. Open http://localhost:8080 → log in as admin
    2. Go to Realm Settings → Authentication tab → Flows → find "browser-passkey"
       - Confirm "password-forms" appears as an ALTERNATIVE branch at priority 30
       - Confirm "passkey-forms" is at priority 20
    3. Go to Realm Settings → Authentication → Required Actions
       - Confirm "Verify Email" is listed and "Default Action" is ON
       - Confirm "Webauthn Register Passwordless" is listed with Default Action OFF
    4. Go to Realm Settings → General
       - Confirm "Browser Flow" shows "browser-passkey"
    5. Go to Clients → confirm "japan-trip-worker" client exists
       - Click it → confirm Service Account Roles tab shows "manage-users" under realm-management
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terraform → KC Admin API | Terraform uses admin credentials from local.tfvars (gitignored) |
| browser-passkey flow | All three branches (cookie, passkey-forms, password-forms) are ALTERNATIVE — no path is forced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Denial of Service | browserFlow flip locking out password users | mitigate | password-forms ALTERNATIVE branch added in same apply; verified via human checkpoint |
| T-07-16 | Elevation of Privilege | terraform apply using admin credentials | mitigate | admin credentials in local.tfvars (gitignored); not in version control |
</threat_model>

<verification>
- `terraform -chdir=terraform/keycloak plan -var-file=local.tfvars` exits 0 with "No changes" (everything already applied)
- KC admin console shows browser-passkey flow with all three ALTERNATIVE branches
- KC admin console shows VERIFY_EMAIL as enabled default action
- KC admin console shows japan-trip-worker client with manage-users role
</verification>

<success_criteria>
- KC-01: VERIFY_EMAIL required action enabled in realm; access_code_lifespan_user_action = 20m
- KC-02: browser_flow = "browser-passkey"; password-forms ALTERNATIVE branch in flow
- KC-03: webauthn-register-passwordless Required Action exists with defaultAction = false (already was; no duplicate)
- BACK-04 (partial): japan-trip-worker client created in KC; service account has manage-users role
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-passkey-login-wire-browser-passkey-keycloak-flow-as-default-/07-07-SUMMARY.md`
</output>
