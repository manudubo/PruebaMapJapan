---
phase: 13-security-audit-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/auth/keycloak.ts
  - backend/src/auth/keycloak.test.ts
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "When a valid token's kid is cached but signature verification fails, the backend refetches JWKS once and retries verification"
    - "A forged token that triggers signature failure does NOT cause more than one JWKS network fetch per request (DoS guard)"
    - "Unit tests cover the signature-failure to refetch-to-success path and the still-fails path"
  artifacts:
    - path: "backend/src/auth/keycloak.ts"
      provides: "verifyJwt with jwksRefreshed guard in signature verification block + __resetJwksCacheForTests export"
      contains: "jwksRefreshed"
    - path: "backend/src/auth/keycloak.test.ts"
      provides: "Test covering JWKS retry on signature failure"
      contains: "JWKS retry on signature failure"
  key_links:
    - from: "verifyJwt (keycloak.ts)"
      to: "getKeycloakJwks (keycloak.ts)"
      via: "jwksCache = null; await getKeycloakJwks(env)"
      pattern: "jwksRefreshed"
---

<objective>
Extend `backend/src/auth/keycloak.ts` to retry the JWKS fetch once when signature verification fails (SEC-02 gap — D-06/D-07). Add a per-request `jwksRefreshed` guard to prevent DoS amplification. Add a `__resetJwksCacheForTests` export so unit tests can reliably reset the module-level cache. Extend `backend/src/auth/keycloak.test.ts` with a new describe block covering this path using mocked `fetch` and `crypto.subtle`.

Purpose: Signing key rotation no longer requires a backend restart — stale-key signature failures trigger one transparent refetch and retry.
Output: `keycloak.ts` (signature block extended + test reset export), `keycloak.test.ts` (new describe block with mocked JWKS and cache reset in beforeEach).
</objective>

<execution_context>
@C:/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Extracted from backend/src/auth/keycloak.ts -->

Module-level cache variable (line 41):
let jwksCache: CachedJwks | null = null;

kid-not-found retry block (lines 209-221 — DO NOT MODIFY):
  const keyMap = await getKeycloakJwks(env);
  let publicKey = keyMap.get(header.kid);
  if (!publicKey) {
    jwksCache = null;
    const refreshedKeyMap = await getKeycloakJwks(env);
    publicKey = refreshedKeyMap.get(header.kid);
    if (!publicKey) {
      throw new Error('JWT signing key not found: kid=' + header.kid);
    }
  }

Signature verification block (lines 223-237 — THIS IS THE TARGET TO REPLACE):
  const signingInput = encodedHeader + '.' + encodedPayload;
  const signingInputBytes = new TextEncoder().encode(signingInput);
  const signatureBytes = base64urlToArrayBuffer(encodedSignature);
  const isValid = await crypto.subtle.verify(
    { name: 'RSASSA-PKCS1-v1_5' },
    publicKey,
    signatureBytes,
    signingInputBytes,
  );
  if (!isValid) {
    throw new Error('JWT signature verification failed');
  }

verifyJwt export signature:
  export async function verifyJwt(token: string, env: Env): Promise<KeycloakJwtPayload>

Existing keycloak.test.ts imports:
  import { describe, it, expect } from 'vitest';
  import { validateAudience } from './keycloak';
  (verifyJwt and __resetJwksCacheForTests are NOT yet imported — add them)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add jwksRefreshed retry guard and cache-reset export to keycloak.ts</name>
  <files>backend/src/auth/keycloak.ts</files>
  <read_first>
    - backend/src/auth/keycloak.ts lines 200-260 — read the full signature verification block and the code after verifyJwt to understand variable scope and current end-of-file position; all variables (signingInputBytes, signatureBytes, publicKey, encodedHeader, encodedPayload, encodedSignature, header.kid, env) are already in scope from earlier in verifyJwt
  </read_first>
  <behavior>
    - `let jwksRefreshed = false` declared BEFORE the first crypto.subtle.verify call
    - `isValid` changed from `const` to `let` so it can be reassigned on retry
    - After `isValid === false` AND `!jwksRefreshed`: set jwksRefreshed = true, null the cache, refetch, get retryKey, if retryKey found re-run crypto.subtle.verify and reassign isValid
    - Do NOT touch the kid-not-found block (lines 209-221)
    - Add `__resetJwksCacheForTests` as the last export in the file (after extractUserInfo and any other exports)
  </behavior>
  <action>
**Change 1:** Replace the signature verification block (lines 223-237) with the retry-guarded version:

  // Verify RS256 signature using Web Crypto API
  const signingInput = `${encodedHeader}.${encodedPayload}`;
  const signingInputBytes = new TextEncoder().encode(signingInput);
  const signatureBytes = base64urlToArrayBuffer(encodedSignature);

  let jwksRefreshed = false;

  let isValid = await crypto.subtle.verify(
    { name: 'RSASSA-PKCS1-v1_5' },
    publicKey,
    signatureBytes,
    signingInputBytes,
  );

  if (!isValid && !jwksRefreshed) {
    // Retry once: invalidate cache and refetch JWKS (handles stale key after rotation)
    jwksRefreshed = true;
    jwksCache = null;
    const retryKeyMap = await getKeycloakJwks(env);
    const retryKey = retryKeyMap.get(header.kid);
    if (retryKey) {
      isValid = await crypto.subtle.verify(
        { name: 'RSASSA-PKCS1-v1_5' },
        retryKey,
        signatureBytes,
        signingInputBytes,
      );
    }
  }

  if (!isValid) {
    throw new Error('JWT signature verification failed');
  }

`jwksRefreshed` is a local variable in verifyJwt() — resets to false on every call. The jwksCache module variable (line 41) and getKeycloakJwks are both in scope within verifyJwt.

**Change 2:** Add this export at the very END of keycloak.ts (after extractUserInfo export):

export function __resetJwksCacheForTests(): void {
  jwksCache = null;
}

This function is used ONLY in unit tests (beforeEach) to reset the module-level cache between test runs. Without it, the second test inherits the populated cache from the first and the fetch call count assertions fail deterministically.
  </action>
  <verify>
    <automated>cd C:\Users\manud\Documents\GitHub\PruebaMapJapan\backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - rg "jwksRefreshed" backend/src/auth/keycloak.ts returns at least 3 lines (let declaration, condition, assignment to true)
    - rg "let isValid" backend/src/auth/keycloak.ts returns a match (changed from const to let)
    - rg "retryKey" backend/src/auth/keycloak.ts returns at least 2 lines
    - rg "__resetJwksCacheForTests" backend/src/auth/keycloak.ts returns a match (the new export)
    - rg "JWT signing key not found" backend/src/auth/keycloak.ts returns exactly 1 match (kid-not-found block unchanged)
    - npx tsc --noEmit exits 0 in the backend directory
  </acceptance_criteria>
  <done>verifyJwt retry guard added; isValid is let; jwksRefreshed flag prevents double-fetch; __resetJwksCacheForTests exported; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JWKS retry unit tests to keycloak.test.ts</name>
  <files>backend/src/auth/keycloak.test.ts</files>
  <read_first>
    - backend/src/auth/keycloak.test.ts — read the full file to understand existing imports and describe blocks; the new block goes at the END of the file
    - backend/src/auth/keycloak.ts — confirm __resetJwksCacheForTests is now exported (from Task 1 above)
  </read_first>
  <behavior>
    - New imports added at the top: vi, beforeEach, afterEach from vitest; verifyJwt and __resetJwksCacheForTests from ./keycloak
    - beforeEach calls __resetJwksCacheForTests() to clear module-level cache, then stubs fetch and crypto.subtle
    - afterEach calls vi.unstubAllGlobals()
    - Test 1 (retry succeeds): crypto.subtle.verify returns false then true; verifyJwt resolves; fetch called exactly 2 times (initial JWKS + retry)
    - Test 2 (retry still fails): crypto.subtle.verify always returns false; verifyJwt rejects; fetch called exactly 2 times (initial JWKS + one retry only — jwksRefreshed guard prevents more)
    - Both tests use a fake JWT constructed with btoa so verifyJwt reaches the signature verification block
    - crypto.subtle.importKey is mocked to return a fake CryptoKey so getKeycloakJwks succeeds
  </behavior>
  <action>
Update the import line at the top of backend/src/auth/keycloak.test.ts:

  Change: import { describe, it, expect } from 'vitest';
  To:     import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

  Add on next line: import { validateAudience, verifyJwt, __resetJwksCacheForTests } from './keycloak';
  (replace the existing single-import line for validateAudience)

Then append this describe block at the END of the file:

describe('verifyJwt — JWKS retry on signature failure (SEC-02)', () => {
  const FAKE_KID = 'test-key-id';

  function makeFakeJwt(kid: string): string {
    const header = btoa(JSON.stringify({ alg: 'RS256', typ: 'JWT', kid }))
      .replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, '');
    const now = Math.floor(Date.now() / 1000);
    const payload = btoa(JSON.stringify({
      iss: 'http://localhost:8080/realms/japan-trip',
      sub: 'test-user-id',
      aud: 'japan-trip-frontend',
      exp: now + 3600,
      nbf: now - 1,
      iat: now,
    })).replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, '');
    return `${header}.${payload}.fakesig`;
  }

  const mockEnv = {
    KEYCLOAK_URL: 'http://localhost:8080',
    KEYCLOAK_REALM: 'japan-trip',
    VALID_AUDIENCES: 'japan-trip-frontend',
    DATABASE_URL: 'postgresql://mock:mock@localhost/mockdb',
    KC_ADMIN_CLIENT_ID: 'japan-trip-worker',
    KC_ADMIN_CLIENT_SECRET: 'mock-secret',
    OTP_SECRET: 'a3f8c2d1e4b7f0a9d6c3e8b1f4a7d0c2e5b8f3a6d9c0e3b6f1a4d7c0e3b6f1',
  };

  const fakeCryptoKey = {} as CryptoKey;

  const fakeJwksJson = {
    keys: [{
      kid: FAKE_KID, kty: 'RSA', alg: 'RS256', use: 'sig',
      n: 'sGb_fake_n_value', e: 'AQAB',
    }],
  };

  beforeEach(() => {
    // MUST call this first: resets module-level jwksCache to null so each test starts clean.
    // Without this, test 2 sees the cache populated by test 1 and the fetch call count is wrong.
    __resetJwksCacheForTests();

    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: async () => fakeJwksJson,
    }));

    vi.stubGlobal('crypto', {
      subtle: {
        importKey: vi.fn().mockResolvedValue(fakeCryptoKey),
        verify: vi.fn(),
      },
    });
  });

  afterEach(() => {
    vi.unstubAllGlobals();
  });

  it('retries JWKS fetch and succeeds when signature verification passes on retry', async () => {
    // First verify call returns false (stale key), second returns true (fresh key after refetch)
    (crypto.subtle.verify as ReturnType<typeof vi.fn>)
      .mockResolvedValueOnce(false)
      .mockResolvedValueOnce(true);

    const token = makeFakeJwt(FAKE_KID);
    const payload = await verifyJwt(token, mockEnv as unknown as import('../types').Env);
    expect(payload.sub).toBe('test-user-id');

    // Cache was null (reset in beforeEach) so initial fetch happens, then retry fetch happens
    expect(vi.mocked(fetch)).toHaveBeenCalledTimes(2);
  });

  it('throws after retry if signature still invalid (not a rotation issue)', async () => {
    // Both verify calls return false — token is genuinely invalid
    (crypto.subtle.verify as ReturnType<typeof vi.fn>).mockResolvedValue(false);

    const token = makeFakeJwt(FAKE_KID);
    await expect(verifyJwt(token, mockEnv as unknown as import('../types').Env))
      .rejects.toThrow('JWT signature verification failed');

    // jwksRefreshed guard: exactly 1 retry fetch, not more
    expect(vi.mocked(fetch)).toHaveBeenCalledTimes(2);
  });
});

Note on fetch call count logic: With __resetJwksCacheForTests() called in beforeEach, jwksCache is null at test start. The initial getKeycloakJwks call fetches (count=1), populates cache. On signature failure, jwksCache = null + getKeycloakJwks refetches (count=2). jwksRefreshed = true prevents any further fetches. Both tests produce count=2. This is deterministic, not flaky.
  </action>
  <verify>
    <automated>cd C:\Users\manud\Documents\GitHub\PruebaMapJapan\backend && npm test -- --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - rg "JWKS retry on signature failure" backend/src/auth/keycloak.test.ts returns a match
    - rg "__resetJwksCacheForTests" backend/src/auth/keycloak.test.ts returns at least 2 matches (import + beforeEach call)
    - rg "toHaveBeenCalledTimes.2." backend/src/auth/keycloak.test.ts returns exactly 2 matches (one per test)
    - rg "retries JWKS fetch and succeeds" backend/src/auth/keycloak.test.ts returns a match
    - rg "throws after retry if signature still invalid" backend/src/auth/keycloak.test.ts returns a match
    - cd backend && npm test exits 0 with ALL tests passing including the new describe block
    - Test output shows 2 PASS results under "verifyJwt — JWKS retry on signature failure (SEC-02)"
  </acceptance_criteria>
  <done>Two new unit tests pass: retry-succeeds and retry-still-fails; fetch called exactly 2 times in both; npm test exits 0 across all backend tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keycloak JWKS endpoint to Backend | Backend fetches public keys from Keycloak; network call under attacker influence |
| JWT Bearer token to Backend verifyJwt | Attacker controls token bytes; can trigger signature failure path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Denial of Service | verifyJwt — JWKS refetch on signature failure | mitigate | jwksRefreshed flag limits refetch to exactly 1 per request — attacker sending N forged tokens triggers at most N Keycloak JWKS fetches, not N squared |
| T-13-06 | Denial of Service | JWKS cache invalidation | mitigate | Cache is only invalidated (jwksCache = null) when signature fails AND jwksRefreshed is false — legitimate traffic is unaffected; TTL-based cache is the primary protection |
| T-13-07 | Elevation of Privilege | Stale signing key causes valid tokens to be permanently rejected | mitigate | Signature-failure retry fetches fresh JWKS once — key rotation does not require backend restart; service availability is maintained |
</threat_model>

<verification>
After task 2:

  cd C:\Users\manud\Documents\GitHub\PruebaMapJapan\backend && npm test -- --reporter=verbose

Expected: All tests pass including:
  "verifyJwt — JWKS retry on signature failure (SEC-02) > retries JWKS fetch and succeeds when signature verification passes on retry"
  "verifyJwt — JWKS retry on signature failure (SEC-02) > throws after retry if signature still invalid (not a rotation issue)"
</verification>

<success_criteria>
- keycloak.ts has jwksRefreshed guard in signature block; isValid is let; __resetJwksCacheForTests is exported
- keycloak.test.ts has 2 new tests; beforeEach calls __resetJwksCacheForTests() to ensure clean cache state
- cd backend && npm test exits 0 with all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/13-security-audit-documentation/13-02-SUMMARY.md`
</output>
