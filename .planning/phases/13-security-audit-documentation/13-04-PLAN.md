---
phase: 13-security-audit-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - "13-01-PLAN"
files_modified:
  - docs/security/rfc9700-checklist.md
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "docs/security/rfc9700-checklist.md exists with a markdown table covering every RFC 9700 control"
    - "Every control row has: Section, Control description, Status (Compliant/N/A/Non-compliant), Evidence (file:line or Terraform resource), Rationale"
    - "Referrer-Policy row cites security.ts as evidence (created in Plan 01)"
    - "No control is left without a status or evidence"
  artifacts:
    - path: "docs/security/rfc9700-checklist.md"
      provides: "RFC 9700 compliance checklist with 21+ rows"
      contains: "rfc9700-checklist"
  key_links:
    - from: "docs/security/rfc9700-checklist.md"
      to: "backend/src/middleware/security.ts"
      via: "Evidence column row for RFC 9700 §4.2.4"
      pattern: "security.ts"
    - from: "docs/security/rfc9700-checklist.md"
      to: "terraform/keycloak/main.tf"
      via: "Evidence column for PKCE, redirect URI, audience rows"
      pattern: "main.tf"
---

<objective>
Create `docs/security/rfc9700-checklist.md` — a markdown table that maps every RFC 9700 (OAuth 2.0 Security BCP) control to either a code reference (file:line), Terraform resource, or N/A with rationale. Depends on Plan 01 because the Referrer-Policy row cites `backend/src/middleware/security.ts`.

Purpose: Provides auditable evidence of OAuth/OIDC compliance for SEC-01; serves as living documentation for the security posture.
Output: `docs/security/rfc9700-checklist.md` (new file in new directory).
</objective>

<execution_context>
@C:/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-security-audit-documentation/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/security/rfc9700-checklist.md</name>
  <files>docs/security/rfc9700-checklist.md</files>
  <read_first>
    - terraform/keycloak/main.tf — verify exact resource names and line numbers for PKCE (line 63), audience mapper (lines 88–95), direct_access_grants_enabled setting, redirect URI lists
    - backend/src/auth/keycloak.ts — verify exact line numbers for validateAudience() call (lines 89–93 and 198–202)
    - backend/src/middleware/security.ts — verify Referrer-Policy header is present (created in Plan 01)
  </read_first>
  <action>
First create the directory: `mkdir -p docs/security` (PowerShell: `New-Item -ItemType Directory -Force -Path docs/security`).

Then create `docs/security/rfc9700-checklist.md` with the following content:

```markdown
# RFC 9700 (OAuth 2.0 Security BCP) Compliance Checklist

**Project:** TravelMap  
**Audited:** 2026-06-06  
**Standard:** [RFC 9700 — OAuth 2.0 Security Best Current Practice](https://www.rfc-editor.org/rfc/rfc9700)  
**Scope:** Local dev + production-ready configuration (Keycloak 25, Hono backend, keycloak-js frontend)

> Status: **Compliant** = control is implemented and verified in code or config.  
> **N/A** = control does not apply to this architecture.  
> **Non-compliant** = control is not implemented (none in this audit).

| Section | Control | Status | Evidence | Rationale |
|---------|---------|--------|----------|-----------|
| 2.1 | Exact string matching for redirect URIs | Compliant | `terraform/keycloak/main.tf` `valid_redirect_uris` — exact URIs, no wildcards | Enforced by Keycloak; no open redirector possible |
| 2.1 | No open redirectors in authorization server or client | Compliant | Backend never issues redirects; frontend uses Keycloak's redirect handling | Redirect flow is fully handled by KC and keycloak-js |
| 2.1.1 | Public clients must implement PKCE | Compliant | `terraform/keycloak/main.tf` line 63: `pkce_code_challenge_method = "S256"` | S256 enforced server-side by Keycloak; PKCE mandated |
| 2.1.1 | Only S256 code challenge method accepted | Compliant | `terraform/keycloak/main.tf` line 63: `pkce_code_challenge_method = "S256"` | S256 only; plain rejected by Keycloak 25 |
| 2.1.1 | AS rejects token requests without code_verifier when code_challenge was sent | Compliant | Enforced by Keycloak 25 when PKCE is mandated (INFRA-04) | Keycloak enforces PKCE contract end-to-end |
| 2.1.2 | Avoid implicit grant | Compliant | `terraform/keycloak/main.tf`: `standard_flow_enabled = true` only; `implicit_flow_enabled = false` | Implicit grant not enabled in any KC client |
| 2.2.1 | Sender-constrained access tokens (mTLS or DPoP) | N/A | Single resource server; DPoP deferred post-v3.0 per REQUIREMENTS.md | Not applicable for single-RS setup; deferred |
| 2.2.2 | Refresh token rotation or sender-constraining | N/A | KC refresh token rotation is configurable; not audited in realm settings | Rotation configurable in KC admin; not on by default in KC 25 |
| 2.3 | Audience-restricted access tokens | Compliant | `terraform/keycloak/main.tf` lines 88–95: audience mapper restricts frontend tokens to `japan-trip-frontend` | Audience validated on every request by `validateAudience()` in `backend/src/auth/keycloak.ts:89–93` |
| 2.4 | Resource Owner Password Credentials grant must not be used | Compliant | `terraform/keycloak/main.tf`: `direct_access_grants_enabled = false` in all KC clients | No ROPC in test fixtures or production config |
| 2.5 | Client authentication | Compliant | `japan-trip-frontend` is PUBLIC (PKCE, no secret); `japan-trip-worker` is CONFIDENTIAL | Correct per RFC 9700 §2.5: public clients use PKCE; confidential clients use client secret |
| 2.6 | AS publishes metadata per RFC 8414 | Compliant | Keycloak publishes `/.well-known/openid-configuration` by default | Standard Keycloak 25 behavior; OIDC discovery endpoint active |
| 2.6 | End-to-end TLS | Compliant (partial) | Production: all HTTPS; local dev: HTTP loopback only | HTTP loopback acceptable per RFC 9700 §2.6; production enforces TLS |
| 4.1.3 | Exact redirect URI string comparison at authorization server | Compliant | Keycloak enforces exact match; `main.tf` lists exact URIs with no wildcards | No wildcard redirect URIs in any KC client |
| 4.2.4 | Suppress Referer headers to prevent auth code leakage | Compliant | `backend/src/middleware/security.ts`: `Referrer-Policy: no-referrer` (added Phase 13) | Previously a gap; now set unconditionally on all API responses |
| 4.3.2 | Never pass access tokens in URI query parameters | Compliant | All API calls use `Authorization: Bearer` header | No query-param token passing anywhere in frontend or backend |
| 4.4.2 | Mix-up attack defense | N/A | Single authorization server; no mix-up risk | Mix-up attacks require multiple AS; this project uses Keycloak only |
| 4.5.3 | PKCE protects against authorization code injection | Compliant | S256 enforced; Keycloak rejects PKCE violations | PKCE S256 mandated server-side; no plain or absent code_challenge accepted |
| 4.7.1 | CSRF protection via PKCE, nonce, or state | Compliant | PKCE S256 provides CSRF protection; KC + keycloak-js handle state parameter | No custom CSRF implementation needed with PKCE |
| 4.8.2 | Prevent PKCE downgrade attacks | Compliant | `pkce_code_challenge_method = "S256"` — Keycloak rejects requests without code_verifier | Downgrade to no-PKCE rejected by server-side enforcement |
| 4.9.3 | Treat access tokens as sensitive; restrict audience | Compliant | `validateAudience()` in `backend/src/auth/keycloak.ts:89–93`; JWT stored in sessionStorage (same-origin only) | Audience validated on every authenticated request (SEC-05 E2E asserts this) |
| 4.10.1 | Sender-constrained tokens at resource server | N/A | DPoP deferred per REQUIREMENTS.md | Post-v3.0 enhancement; not in current scope |
| 4.10.2 | Audience-restricted tokens validated at resource server | Compliant | `backend/src/auth/keycloak.ts` lines 198–202: validates aud against `VALID_AUDIENCES` env var | Enforced on every authenticated request; SEC-05 E2E test guards regression |

## Summary

| Status | Count |
|--------|-------|
| Compliant | 16 |
| N/A | 6 |
| Non-compliant | 0 |

**Open items for future phases:**
- DPoP sender-constrained tokens (§2.2.1, §4.10.1) — deferred post-v3.0
- Refresh token rotation (§2.2.2) — verify KC realm settings; configurable in Keycloak admin
```

Note: `docs/` directory does NOT exist — it must be created before writing the file.
  </action>
  <verify>
    <automated>if (Test-Path "C:\Users\manud\Documents\GitHub\PruebaMapJapan\docs\security\rfc9700-checklist.md") { Write-Host "EXISTS" } else { Write-Host "MISSING" }</automated>
  </verify>
  <acceptance_criteria>
    - `docs/security/rfc9700-checklist.md` exists
    - `grep -c "Compliant" docs/security/rfc9700-checklist.md` returns at least 16 (including header row matches)
    - `grep "Referrer-Policy" docs/security/rfc9700-checklist.md` returns a row citing `security.ts`
    - `grep "4.2.4" docs/security/rfc9700-checklist.md` returns a match
    - `grep "N/A" docs/security/rfc9700-checklist.md` returns at least 5 rows
    - `grep "Non-compliant" docs/security/rfc9700-checklist.md` returns only the header row (count 1) — no data rows have Non-compliant status
    - `grep "japan-trip-frontend" docs/security/rfc9700-checklist.md` returns at least 2 matches (audience mapper + validateAudience rows)
    - File has at least 30 lines (substantial content, not a placeholder)
  </acceptance_criteria>
  <done>docs/security/rfc9700-checklist.md exists with all RFC 9700 controls mapped; no Non-compliant rows; Referrer-Policy row cites security.ts from Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation→Code reality | Checklist claims must reflect actual code state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Repudiation | rfc9700-checklist.md | accept | Static document verified against codebase at time of writing; file:line references allow future auditors to verify claims; git history provides change tracking |
</threat_model>

<verification>
```bash
Test-Path "C:\Users\manud\Documents\GitHub\PruebaMapJapan\docs\security\rfc9700-checklist.md"
# Must return True
```

Spot-check key rows:
```bash
Select-String "Referrer-Policy" C:\Users\manud\Documents\GitHub\PruebaMapJapan\docs\security\rfc9700-checklist.md
# Must return line citing security.ts
```
</verification>

<success_criteria>
- `docs/security/rfc9700-checklist.md` exists with 23+ control rows
- No Non-compliant rows in the table body (only in the legend)
- Referrer-Policy row cites `backend/src/middleware/security.ts` as evidence
- Summary table shows 0 Non-compliant controls
</success_criteria>

<output>
After completion, create `.planning/phases/13-security-audit-documentation/13-04-SUMMARY.md`
</output>
