---
phase: 01-security-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/modules/dom.ts
  - frontend/package.json
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "frontend/src/modules/dom.ts exists with exactly two named exports: setText and setStyle"
    - "dompurify is listed in frontend/package.json dependencies"
    - "@types/dompurify is NOT in frontend/package.json"
    - "dom.test.ts turns GREEN; popup.test.ts advances from import-error RED to assertion-failure RED"
  artifacts:
    - path: "frontend/src/modules/dom.ts"
      provides: "setText(el, text) and setStyle(el, prop, value) — injection-safe DOM helpers"
      exports: ["setText", "setStyle"]
      min_lines: 6
    - path: "frontend/package.json"
      provides: "dompurify dependency for Wave 3 popup sanitization"
      contains: '"dompurify"'
  key_links:
    - from: "frontend/tests/dom.test.ts"
      to: "frontend/src/modules/dom.ts"
      via: "import { setText, setStyle } from '@/modules/dom'"
      pattern: "from '@/modules/dom'"
    - from: "frontend/src/pages/tripDetail.ts (Wave 3)"
      to: "frontend/src/modules/dom.ts"
      via: "import { setText, setStyle } from '@/modules/dom'"
      pattern: "setText|setStyle"
---

<objective>
Create the dom.ts injection-safe DOM helper and install DOMPurify. These are the shared foundations that all three Wave 3 page edit plans consume.

Purpose: dom.ts establishes the only safe text/style mutation API for the frontend. DOMPurify installation unblocks popup sanitization in tripDetail.ts and map.ts.

Output: dom.ts (new file, 2 exported functions), dompurify added to package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-PATTERNS.md

<interfaces>
<!-- Module structure analog: frontend/src/modules/theme.ts -->
<!-- Named exports only, no default export, explicit param/return types, no comments (per CLAUDE.md) -->

```typescript
// frontend/src/modules/dom.ts — full file content (locked by D-02)
export function setText(el: Element, text: string): void {
  el.textContent = text;
}

export function setStyle(el: HTMLElement, prop: string, value: string): void {
  el.style.setProperty(prop, value);
}
```

<!-- Downstream consumers (Wave 3 plans) import as: -->
<!--   import { setText, setStyle } from '@/modules/dom'; -->
<!-- The '@/' alias resolves to 'frontend/src/' per vitest.config.ts -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create frontend/src/modules/dom.ts</name>
  <files>frontend/src/modules/dom.ts</files>
  <read_first>
    - frontend/tests/dom.test.ts (the RED test contract this file must satisfy — read FIRST)
    - frontend/src/modules/theme.ts (module structure analog: named exports, explicit types, no default export)
    - frontend/vitest.config.ts (confirm '@' alias and test include patterns)
  </read_first>
  <behavior>
    - setText(el, 'hello') → el.textContent === 'hello'
    - setStyle(el, 'background-image', "url('/img.jpg')") → el.style.setProperty called with ('background-image', "url('/img.jpg')")
    - setText must NOT assign to innerHTML — the dom.test.ts spy on innerHTML.set must not fire
    - setStyle must NOT assign to innerHTML — the dom.test.ts spy on innerHTML.set must not fire
  </behavior>
  <action>
Create the file `frontend/src/modules/dom.ts` with this exact content — no more, no less (locked by D-02):

```typescript
export function setText(el: Element, text: string): void {
  el.textContent = text;
}

export function setStyle(el: HTMLElement, prop: string, value: string): void {
  el.style.setProperty(prop, value);
}
```

No imports, no comments, no default export. TypeScript strict mode is enforced — explicit param and return types are required (already present above).
  </action>
  <verify>
    <automated>cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run test:run -- --reporter=verbose 2>&1 | grep -E "dom.test|setText|setStyle|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/modules/dom.ts
    - `grep -c "export function setText" frontend/src/modules/dom.ts` returns 1
    - `grep -c "export function setStyle" frontend/src/modules/dom.ts` returns 1
    - `grep -c "export default" frontend/src/modules/dom.ts` returns 0
    - `grep -c "innerHTML" frontend/src/modules/dom.ts` returns 0
    - dom.test.ts turns GREEN (4/4 tests pass) when npm run test:run is executed
  </acceptance_criteria>
  <done>dom.ts exists with two named exports; dom.test.ts is GREEN (4 passing tests)</done>
</task>

<task type="auto">
  <name>Task 2: Install dompurify in frontend package.json</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (current dependencies — confirm dompurify is absent, confirm @types/dompurify is absent)
    - frontend/tests/popup.test.ts (the RED stub imports 'dompurify' and real popup builders — this install resolves the dompurify import but popup.test.ts will still FAIL on XSS assertions until Plans 06+07 run)
  </read_first>
  <action>
Run the following command from the frontend directory to install DOMPurify 3.4.1. Do NOT install @types/dompurify — it is deprecated; DOMPurify 3.x ships its own type definitions in dist/purify.cjs.d.ts (verified: npm registry).

```bash
cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm install dompurify@3.4.1
```

After install, verify package.json has `"dompurify": "^3.4.1"` (or exact `"3.4.1"`) in dependencies. Do not add `@types/dompurify` — if npm prompts or suggests it, decline.

The install also updates package-lock.json. Both files are expected to change.
  </action>
  <verify>
    <automated>cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run test:run 2>&1 | grep -E "popup.test|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "dompurify" frontend/package.json` returns a line containing "dompurify" in the dependencies block
    - `grep "@types/dompurify" frontend/package.json` returns empty (exit code 1) — forbidden
    - `grep -c "@types/dompurify" frontend/package.json` returns 0
    - popup.test.ts advances from "Cannot find module 'dompurify'" RED to XSS-assertion FAIL RED — the test run still reports FAIL for popup.test.ts but the failure reason changes from import error to assertion failure (expect(result).not.toContain('<script>') fails because buildPopup/buildHotelPopup/createPopupContent/createHotelPopup do not yet call DOMPurify)
  </acceptance_criteria>
  <done>dompurify listed in frontend/package.json dependencies; popup.test.ts moves from import-error RED to assertion-failure RED (4 tests fail on XSS assertion, not module resolution) — popup.test.ts turns GREEN only after Plans 06 and 07 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled string → DOM | All text mutation goes through setText/setStyle; no raw innerHTML assignment |
| npm registry → frontend bundle | dompurify package installed — must be the canonical library, no typosquat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | dom.ts setText | mitigate | Uses el.textContent — browser never interprets as HTML regardless of content |
| T-01-02 | Tampering | dom.ts setStyle | mitigate | Uses el.style.setProperty — CSS parser sandboxes the value; expression() injection requires IE6 |
| T-01-03 | Tampering | dompurify package install | mitigate | Pin to 3.4.1; acceptance_criteria forbids @types/dompurify to prevent confusion with deprecated stub |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run test:run -- --reporter=verbose 2>&1 | grep -E "dom.test|popup.test|PASS|FAIL"
```

Expected: dom.test.ts PASS (4/4). popup.test.ts FAIL — but the failure is XSS assertion failure (expect(result).not.toContain('<script>') fails), NOT "Cannot find module". popup.test.ts turns GREEN only after Plans 06 and 07 add DOMPurify.sanitize() to the popup builders.
</verification>

<success_criteria>
- frontend/src/modules/dom.ts exists with setText and setStyle as the only exports
- dompurify@3.4.1 in frontend/package.json; @types/dompurify absent
- All 4 tests in dom.test.ts GREEN
- popup.test.ts FAIL (4 XSS assertion failures, not import errors) — this is expected and correct; GREEN state deferred to Plans 06+07
- TypeScript build succeeds: `cd frontend && npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-03-SUMMARY.md`
</output>
