---
phase: 01-security-hardening
plan: 07
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - frontend/src/modules/map.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "No innerHTML assignment in map.ts receives a user-controlled string directly"
    - "createPopupContent and createHotelPopup return DOMPurify.sanitize(html)"
    - "Inline style injections (day.color, markerColor) set via setStyle"
    - "Text content (day.label, activity.name, hotel.name, city.name) set via setText or textContent"
    - "Overview map popup at line 305 is sanitized"
  artifacts:
    - path: "frontend/src/modules/map.ts"
      provides: "SEC-01 + SEC-02 hardened map module"
      contains: "import DOMPurify from 'dompurify'"
  key_links:
    - from: "frontend/src/modules/map.ts"
      to: "frontend/src/modules/dom.ts"
      via: "import { setText, setStyle } from '@/modules/dom'"
      pattern: "from '@/modules/dom'"
    - from: "frontend/src/modules/map.ts"
      to: "dompurify"
      via: "DOMPurify.sanitize(html) in createPopupContent and createHotelPopup"
      pattern: "DOMPurify.sanitize"
---

<objective>
Replace all innerHTML injection sites in map.ts with injection-safe alternatives: dom.ts helpers for text/style content, DOMPurify for Leaflet popup HTML strings in BOTH createPopupContent AND createHotelPopup.

Purpose: map.ts mirrors tripDetail.ts's injection surface but for the Leaflet map rendering path. Per the blocker resolution in the planning context, D-04 is EXPANDED to cover BOTH map.ts popup builders — the phase success criterion "all Leaflet popups pass HTML through DOMPurify" requires this.

Output: map.ts with all 7 injection sites fixed, importing dom.ts helpers and DOMPurify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/phases/01-security-hardening/01-PATTERNS.md

<interfaces>
<!-- dom.ts contracts (created in Plan 03): -->
```typescript
// frontend/src/modules/dom.ts
export function setText(el: Element, text: string): void  // sets el.textContent = text
export function setStyle(el: HTMLElement, prop: string, value: string): void  // sets el.style.setProperty(prop, value)
```

<!-- Safe imperative DOM analog already in map.ts (lines 85-93) — replicate this pattern: -->
```typescript
const btn = document.createElement('button');
btn.className = 'day-btn' + (day.hasOptions ? ' has-options' : '');
btn.textContent = day.label;
btn.dataset.day = dateKey;
btn.setAttribute('role', 'tab');
btn.setAttribute('aria-selected', 'false');
if (day.hasOptions) btn.title = 'Este día tiene opciones alternativas';
daySelector.appendChild(btn);
```

<!-- Injection sites in map.ts from PATTERNS.md lines 177-201: -->
<!-- Line 29-33: createMarkerIcon — color and label in DivIcon html string -->
<!-- Line 45-53: createPopupContent — day.label, activity.optional, activity.name, activity.notes (SEC-02) -->
<!-- Line 56-62: createHotelPopup — hotel.name (SEC-02) -->
<!-- Line 236: dayGroup.innerHTML — day.color, day.label -->
<!-- Line 260: item.innerHTML — markerColor, markerLabel, activity.name, noteText -->
<!-- Line 271-273: hotelInfo.innerHTML — hotel.name -->
<!-- Line 305: overview map popup — city.name, city.dates, city.link -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix createPopupContent and createHotelPopup with DOMPurify (SEC-02)</name>
  <files>frontend/src/modules/map.ts</files>
  <read_first>
    - frontend/src/modules/map.ts (lines 1-80 — imports, createMarkerIcon lines 29-33, createPopupContent lines 45-53, createHotelPopup lines 56-62)
    - frontend/tests/popup.test.ts (confirm GREEN state from Plan 03 — DOMPurify is importable)
    - frontend/src/modules/dom.ts (verify setText/setStyle available from Plan 03)
  </read_first>
  <behavior>
    - createPopupContent(activity, day, mapsUrl) returns a string from DOMPurify.sanitize(html) — user-controlled strings in activity.name, activity.optional, activity.notes, day.label cannot survive as script/event handlers
    - createHotelPopup(hotel, day) returns a string from DOMPurify.sanitize(html) — hotel.name cannot survive as script/event handler
    - DOMPurify default config preserves SVG elements with stroke/viewBox attributes (no xlink:href in these templates)
  </behavior>
  <action>
**Step 1 — Add two imports at the top of map.ts:**

```typescript
import DOMPurify from 'dompurify';
import { setText, setStyle } from '@/modules/dom';
```

Place alongside existing imports. Both are needed — setText/setStyle for Task 2. TypeScript noUnusedLocals requires both to be used.

**Step 2 — Fix createPopupContent (lines 45-53):**

Find the function `createPopupContent` (or equivalent popup builder at ~line 45). Its last line returns the assembled html string. Change:

```typescript
return html;
```

to:

```typescript
return DOMPurify.sanitize(html);
```

D-04 EXPANDED: This function is confirmed as an SEC-02 target. Apply DOMPurify to the full html string at the return point — same pattern as buildPopup in tripDetail.ts (Plan 06).

**Step 3 — Fix createHotelPopup (lines 56-62):**

Same change — find `return html;` (or equivalent) at the end of `createHotelPopup` and change to:

```typescript
return DOMPurify.sanitize(html);
```

D-04 EXPANDED: This function is the hotel popup builder in map.ts. User-controlled `hotel.name` is in the template. Apply DOMPurify.
  </action>
  <verify>
    <automated>cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run build 2>&1 | grep -E "error|Error|TS" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import DOMPurify from 'dompurify'" frontend/src/modules/map.ts` returns 1
    - `grep -c "import { setText, setStyle } from '@/modules/dom'" frontend/src/modules/map.ts` returns 1
    - `grep -c "DOMPurify.sanitize" frontend/src/modules/map.ts` returns 2 (one in createPopupContent, one in createHotelPopup)
    - `grep -n "return html" frontend/src/modules/map.ts` returns empty — no raw return of unsanitized html in popup builders
    - Build exits 0 (TypeScript strict mode)
  </acceptance_criteria>
  <done>DOMPurify imported and applied in both createPopupContent and createHotelPopup; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Replace all remaining innerHTML injection sites in map.ts (SEC-01)</name>
  <files>frontend/src/modules/map.ts</files>
  <read_first>
    - frontend/src/modules/map.ts (full file — read current state after Task 1; injection sites at ~lines 29-33, 236, 260, 271-273, 305)
    - frontend/src/modules/map.ts lines 85-93 (safe createElement analog — replicate this pattern for all replacements)
  </read_first>
  <action>
Fix each remaining injection site from PATTERNS.md. Read current file first — line numbers may shift after Task 1.

**Site 1 — createMarkerIcon DivIcon html (~lines 29-33):**
`html: \`<div ...>${color}...${label}</div>\``
`color` is `day.color` (user-controlled CSS value). `label` is numeric index (safe as String(idx+1)) or `activity.optional` (user string).
Replace: for numeric labels, `String(idx + 1)` is safe (always a number — not user input). For `color`, use `style.setProperty` AFTER icon creation if Leaflet DivIcon allows DOM access, OR construct the DivIcon html with only the static structure and set the color style separately. If the DivIcon html must be a string (Leaflet requires it for DivIcon), and `label` can be a user string: sanitize it via `DOMPurify.sanitize(String(label))` — single-value sanitize is acceptable for this case. For `color` in a style attribute in the html string: use a CSS custom property or omit from html and set via class name, OR accept that `style.setProperty` in the CSS parser prevents script injection (D-03 decision).

**Site 2 — dayGroup render (~line 236):**
`dayGroup.innerHTML = \`...${day.color}...${day.label}...\``
Replace: build child elements imperatively. `setStyle(colorDot, 'background-color', day.color)` for color. `setText(labelEl, day.label)` for label. Use createElement + appendChild. Match existing structure from the template (class names, element types).

**Site 3 — legend item render (~line 260):**
`item.innerHTML = \`...\`` with `markerColor`, `markerLabel`, `activity.name`, `noteText`.
Replace: use `setStyle` for color, `setText` for text content. Build child elements with createElement. Follow the safe analog from lines 85-93 of the same file.

**Site 4 — hotelInfo render (~lines 271-273):**
`hotelInfo.innerHTML = \`...${hotel.name}...\``
Replace: `setText(hotelNameEl, hotel.name)`. Build the hotel info container using createElement + appendChild.

**Site 5 — overview map popup (~line 305):**
Popup string with `city.name`, `city.dates`, `city.link`.
Replace: wrap the assembled popup html string with `DOMPurify.sanitize(html)` before passing to `bindPopup()`. `city.link` is a URL — sanitize removes javascript: URLs by default.

**After all replacements:**
Run `grep -n "innerHTML" frontend/src/modules/map.ts` — zero assignment targets (`=`) with user-controlled string interpolation are permitted.
  </action>
  <verify>
    <automated>cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.innerHTML\s*=" frontend/src/modules/map.ts` returns empty OR only lines where RHS is a literal/static string with no user-controlled template interpolation
    - `grep -c "setText\|setStyle" frontend/src/modules/map.ts` returns at least 5 (one per text/style injection site replaced)
    - `grep -c "DOMPurify.sanitize" frontend/src/modules/map.ts` returns at least 3 (createPopupContent, createHotelPopup from Task 1 + overview popup from this task)
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm run test:run` shows dom.test.ts and popup.test.ts still GREEN
  </acceptance_criteria>
  <done>All innerHTML injection sites in map.ts replaced; 3+ DOMPurify.sanitize calls; build passes; no test regression</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Leaflet map rendering | Map data (day labels, activity names, hotel names, city names, colors) enters the DOM through Leaflet's innerHTML setter inside bindPopup |
| user-controlled CSS value → DivIcon style | day.color enters a style attribute in the Leaflet DivIcon html string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | createPopupContent (SEC-02) | mitigate | DOMPurify.sanitize applied — activity.name, activity.notes, day.label, activity.optional cannot inject script via Leaflet bindPopup innerHTML |
| T-07-02 | Tampering | createHotelPopup (SEC-02) | mitigate | DOMPurify.sanitize applied — hotel.name cannot inject script via Leaflet bindPopup innerHTML |
| T-07-03 | Tampering | dayGroup/item/hotelInfo render (SEC-01) | mitigate | setText/setStyle — text content and style values sandboxed; no HTML interpretation |
| T-07-04 | Tampering | overview popup city.link (SEC-02) | mitigate | DOMPurify.sanitize strips javascript: URLs by default — link injection blocked |
| T-07-05 | Tampering | createMarkerIcon DivIcon label | mitigate | Numeric labels are Number→String (safe); user-string optional labels sanitized via DOMPurify single-value or restricted path |
</threat_model>

<verification>
After both tasks:

```bash
cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run build 2>&1 | tail -3
cd /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend && npm run test:run -- --reporter=verbose 2>&1 | grep -E "PASS|FAIL"
grep -c "DOMPurify.sanitize" /c/Users/manud/Documents/GitHub/PruebaMapJapan/frontend/src/modules/map.ts
```

Expected: build exits 0; all tests GREEN; DOMPurify.sanitize count >= 3.
</verification>

<success_criteria>
- Zero `el.innerHTML = user_string` patterns in map.ts
- DOMPurify.sanitize called in createPopupContent, createHotelPopup, and overview popup (minimum 3 occurrences)
- setText and setStyle used for all text/style content
- TypeScript build succeeds
- All existing frontend tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-07-SUMMARY.md`
</output>
