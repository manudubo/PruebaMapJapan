---
phase: 10-design-tokens-idp-theme
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - keycloak/themes/japan-trip/login/resources/css/login.css
  - keycloak/themes/japan-trip/account/resources/css/account.css
autonomous: true
requirements: [DESIGN-01, DESIGN-02]

must_haves:
  truths:
    - "login.css has no hardcoded hex/rgba values in component rules (all replaced with var(--jp-*))"
    - "account.css has no hardcoded hex/rgba values in component rules (all replaced with var(--jp-*))"
    - "login.css :root contains --jp-bg-dark and --jp-white as new tokens"
    - "account.css :root contains --jp-accent-subtle and --jp-white as new tokens"
    - "DESIGN-02 is still satisfied: KC login page shows Inter font, border-radius 0, no KC logo"
    - "passkeys.spec.ts passes unchanged (no KC template regressions)"
  artifacts:
    - path: "keycloak/themes/japan-trip/login/resources/css/login.css"
      provides: "login.css with zero hardcoded hex in component rules"
      contains: "--jp-bg-dark"
    - path: "keycloak/themes/japan-trip/account/resources/css/account.css"
      provides: "account.css with zero hardcoded hex in component rules"
      contains: "--jp-white"
  key_links:
    - from: "login.css component rules"
      to: "login.css :root --jp-* tokens"
      via: "var(--jp-white) !important"
      pattern: "var\\(--jp-"
---

<objective>
Fix the 4 residual hardcoded values in login.css component rules and the 2 in account.css component rules. Both files already use --jp-* tokens for most rules; this plan closes the remaining gaps to satisfy DESIGN-01.

login.css requires two new tokens added to its own :root block (--jp-bg-dark, --jp-white), consistent with D-02's independent-definitions constraint. account.css requires the same approach (--jp-accent-subtle, --jp-white added to its :root).

DESIGN-02 (KC login page visual match) is already satisfied — no visual/structural changes are made here. This plan is limited to replacing hardcoded values with token references.

Purpose: Complete DESIGN-01 compliance across all KC CSS files.
Output: login.css and account.css with no hardcoded hex in component rules.
</objective>

<execution_context>
@C:/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-design-tokens-idp-theme/10-CONTEXT.md
@.planning/phases/10-design-tokens-idp-theme/10-PATTERNS.md
@.planning/phases/10-design-tokens-idp-theme/10-UI-SPEC.md

<interfaces>
<!-- login.css current :root block (lines 8–24) — tokens already present: -->
:root {
  --jp-bg: #f5f5f7;
  --jp-surface: #ffffff;
  --jp-surface-dark: #1c1c1e;
  --jp-surface-raised-dark: #2c2c2e;
  --jp-text: #1d1d1f;
  --jp-text-dark: #f5f5f7;
  --jp-text-secondary: #515154;
  --jp-text-tertiary: #86868b;
  --jp-border: rgba(0, 0, 0, 0.1);
  --jp-border-strong: #d0d0d5;
  --jp-border-dark: rgba(255, 255, 255, 0.18);
  --jp-accent: #0071e3;
  --jp-accent-hover: #0077ed;
  --jp-danger: #d70015;
  --jp-font: 'Inter', -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
}

<!-- New tokens to ADD to login.css :root: -->
--jp-bg-dark: #000000;
--jp-white: #ffffff;

<!-- login.css residual hardcoded values to fix: -->
Line ~40:  #000000 in @media (prefers-color-scheme: dark) body background
           → var(--jp-bg-dark) !important
Line ~58:  #ffffff in .card-pf, #kc-form-wrapper background
           → var(--jp-surface) !important  (token already in :root at line 10)
Line ~171: #ffffff in .btn-primary color
           → var(--jp-white) !important
Line ~273: rgba(0,0,0,0.1) in hr, .login-pf-signup border-color
           → var(--jp-border) !important  (token already in :root at line 17)

<!-- account.css residual hardcoded values to fix: -->
Line ~54:  rgba(0, 113, 227, 0.08) in .pf-v5-c-nav__link active state background
           → var(--jp-accent-subtle) !important  (NEW token — must add to account.css :root)
Line ~67:  #ffffff in .pf-v5-c-button.pf-m-primary color
           → var(--jp-white) !important  (NEW token — must add to account.css :root)

<!-- New tokens to ADD to account.css :root: -->
--jp-accent-subtle: rgba(0, 113, 227, 0.08);
--jp-white: #ffffff;

<!-- KC PatternFly override pattern: ALL property values in KC CSS files use !important.
     Preserve existing !important when replacing hardcoded values with var() tokens. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix login.css residual hardcoded values</name>
  <files>keycloak/themes/japan-trip/login/resources/css/login.css</files>
  <read_first>
    - keycloak/themes/japan-trip/login/resources/css/login.css (read full file — especially :root block lines 8–24 to see existing tokens, and component rules around lines 40, 58, 171, 273)
  </read_first>
  <action>
Two-step change:

**Step 1: Add two new tokens to login.css :root block.**

Inside the existing :root { ... } block (at lines 8–24), append these two new token definitions at the end of the block (before the closing }):
  --jp-bg-dark: #000000;
  --jp-white: #ffffff;

D-02 constraint: login.css maintains independent token definitions — do NOT create a shared import file.

**Step 2: Replace the 4 residual hardcoded values in component rules.**

Each replacement must preserve the `!important` declaration that the original line uses:

1. Line ~40: Inside `@media (prefers-color-scheme: dark)` block, body background property:
   Change: `background: #000000 !important;`  (or similar)
   To:     `background: var(--jp-bg-dark) !important;`

2. Line ~58: Inside `.card-pf, #kc-form-wrapper` rule (or similar card selector), background property:
   Change: `background: #ffffff !important;`
   To:     `background: var(--jp-surface) !important;`
   Note: --jp-surface is already in :root with value #ffffff

3. Line ~171: Inside `.btn-primary` or `.pf-c-button` color property:
   Change: `color: #ffffff !important;`
   To:     `color: var(--jp-white) !important;`

4. Line ~273: Inside `hr, .login-pf-signup` border-color property:
   Change: `border-color: rgba(0,0,0,0.1) !important;` (or border: 1px solid rgba(0,0,0,0.1))
   To:     use `var(--jp-border)` for the rgba(0,0,0,0.1) part
   Note: --jp-border is already in :root with value rgba(0, 0, 0, 0.1)

IMPORTANT — do NOT modify:
- The :root { } token definition block itself (other than the two appended tokens)
- The @media (prefers-color-scheme: dark) :root override (if any) — this is a token definition
- Any !important rules for border-radius, font, or logo hiding — these implement DESIGN-02 and must remain unchanged
- passkey AIA templates are in login/login-idp-review-profile.ftl etc. — this plan only touches the CSS file
  </action>
  <verify>
    <automated>
      rg "#[0-9a-fA-F]{3,8}\b" keycloak/themes/japan-trip/login/resources/css/login.css -n
    </automated>
    Expected: only lines within the :root { } block (token definitions). No hits in component rules.

    <automated>
      rg "--jp-bg-dark|--jp-white" keycloak/themes/japan-trip/login/resources/css/login.css
    </automated>
    Expected: at least two lines — one definition in :root each, plus usage references in component rules.
  </verify>
  <acceptance_criteria>
    - `rg "--jp-bg-dark" keycloak/themes/japan-trip/login/resources/css/login.css` returns at least 2 lines (definition + usage)
    - `rg "--jp-white" keycloak/themes/japan-trip/login/resources/css/login.css` returns at least 2 lines (definition + usage)
    - `rg "var\(--jp-surface\)" keycloak/themes/japan-trip/login/resources/css/login.css` returns at least 1 match (.card-pf usage)
    - `rg "var\(--jp-border\)" keycloak/themes/japan-trip/login/resources/css/login.css` returns at least 1 match (hr/divider usage)
    - `rg "kc-logo|kc-logo-text|kc-header" keycloak/themes/japan-trip/login/resources/css/login.css` returns matches (DESIGN-02 logo-hiding rules untouched)
    - `rg "border-radius.*0.*important" keycloak/themes/japan-trip/login/resources/css/login.css` returns matches (DESIGN-02 radius rules untouched)
  </acceptance_criteria>
  <done>
    - login.css :root contains --jp-bg-dark: #000000 and --jp-white: #ffffff
    - All 4 hardcoded component-rule values replaced with var(--jp-*) references
    - All !important declarations preserved
    - No DESIGN-02 selectors modified (border-radius, font, logo-hiding rules unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix account.css residual hardcoded values + passkey regression guard</name>
  <files>keycloak/themes/japan-trip/account/resources/css/account.css</files>
  <read_first>
    - keycloak/themes/japan-trip/account/resources/css/account.css (read full file — especially the :root block lines 1–15 for existing tokens, and component rules around lines 54, 67)
  </read_first>
  <action>
Two-step change:

**Step 1: Add two new tokens to account.css :root block.**

Inside the existing :root { ... } block, append these two definitions:
  --jp-accent-subtle: rgba(0, 113, 227, 0.08);
  --jp-white: #ffffff;

D-02 independent definitions constraint applies here too — account.css maintains its own :root.

**Step 2: Replace the 2 residual hardcoded values in component rules.**

1. Line ~54: `.pf-v5-c-nav__link:hover, .pf-v5-c-nav__link.pf-m-current, .pf-v5-c-nav__link.pf-m-current::after` — background property:
   Change: `background: rgba(0, 113, 227, 0.08) !important;`
   To:     `background: var(--jp-accent-subtle) !important;`

2. Line ~67: `.pf-v5-c-button.pf-m-primary` — color property:
   Change: `color: #ffffff !important;`
   To:     `color: var(--jp-white) !important;`

After all file changes, run the passkey regression guard (no further file modifications):

The passkeys Playwright spec uses KC login pages and verifies the AIA (Additional Info Required) flow. Any unintentional CSS change that breaks form layout could break these tests. Run:
  cd tests && npx playwright test e2e/passkeys.spec.ts --project=chromium-passkeys

CRITICAL: Do NOT set SKIP_REAL_AUTH environment variable — even SKIP_REAL_AUTH=0 is truthy in JS (!!"0" === true), which would silently skip all passkey tests and produce a false "pass". Leave SKIP_REAL_AUTH unset. Requires KC running (docker-compose up).

If KC is not running at test time, document this in the summary as "passkeys regression deferred — KC not running; to be verified with docker-compose up before phase gate."
  </action>
  <verify>
    <automated>
      rg "#[0-9a-fA-F]{3,8}\b" keycloak/themes/japan-trip/account/resources/css/account.css -n
    </automated>
    Expected: only lines within the :root { } block (token definitions). No hits in component rules.

    <automated>
      rg "--jp-accent-subtle|--jp-white" keycloak/themes/japan-trip/account/resources/css/account.css
    </automated>
    Expected: definition in :root plus usage reference(s) in component rules.
  </verify>
  <acceptance_criteria>
    - `rg "--jp-accent-subtle" keycloak/themes/japan-trip/account/resources/css/account.css` returns at least 2 lines (definition + usage)
    - `rg "--jp-white" keycloak/themes/japan-trip/account/resources/css/account.css` returns at least 2 lines (definition + usage)
    - `rg "rgba\(0, 113, 227" keycloak/themes/japan-trip/account/resources/css/account.css` returns empty (hardcoded rgba removed)
    - `rg "#ffffff" keycloak/themes/japan-trip/account/resources/css/account.css` returns empty or only :root lines
    - passkeys.spec.ts ran and passed, OR documented as deferred with KC unavailability note in SUMMARY
  </acceptance_criteria>
  <done>
    - account.css :root contains --jp-accent-subtle and --jp-white
    - Both component-rule hardcoded values replaced with var(--jp-*) references
    - passkeys.spec.ts ran and passed (or KC not available — documented in summary)
    - login.css DESIGN-02 selectors verified untouched
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KC CSS files | Developer-authored static CSS served by Keycloak; no user input flows into these files at runtime |
| PatternFly override pattern | !important declarations override PatternFly defaults; must not be removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | login.css PatternFly overrides | mitigate | Preserve all existing !important declarations when replacing hardcoded values; verify via rg after change that logo-hiding and border-radius rules are unchanged |
| T-10-06 | Tampering | login.css/account.css token names | accept | Token names are compile-time CSS constants; no user input path; independent :root definitions per D-02 |
| T-10-07 | Tampering | KC passkey AIA templates | mitigate | Run passkeys.spec.ts after CSS changes; CSS file changes only (no FTL templates modified in this plan); AIA templates are frozen per STATE.md blocker |
</threat_model>

<verification>
Phase-gate verification for this plan:

1. login.css has no hardcoded hex in component rules:
   rg "#[0-9a-fA-F]{3,8}\b" keycloak/themes/japan-trip/login/resources/css/login.css -n
   → only lines inside :root { } block

2. account.css has no hardcoded hex in component rules:
   rg "#[0-9a-fA-F]{3,8}\b" keycloak/themes/japan-trip/account/resources/css/account.css -n
   → only lines inside :root { } block

3. login.css new tokens present:
   rg "--jp-bg-dark|--jp-white" keycloak/themes/japan-trip/login/resources/css/login.css
   → at least 2 lines (one definition each)

4. account.css new tokens present:
   rg "--jp-accent-subtle|--jp-white" keycloak/themes/japan-trip/account/resources/css/account.css
   → at least 2 lines (one definition each)

5. DESIGN-02 regression guard:
   rg "kc-logo|border-radius.*0" keycloak/themes/japan-trip/login/resources/css/login.css
   → selectors still present

6. idp-theme.spec.ts (requires KC running):
   cd tests && npx playwright test e2e/idp-theme.spec.ts --project=chromium
   → all pass

7. passkeys.spec.ts (requires KC running, no SKIP_REAL_AUTH):
   cd tests && npx playwright test e2e/passkeys.spec.ts --project=chromium-passkeys
   → all pass
</verification>

<success_criteria>
- login.css :root has --jp-bg-dark and --jp-white; all 4 component-rule hardcoded values replaced
- account.css :root has --jp-accent-subtle and --jp-white; both component-rule hardcoded values replaced
- All !important declarations from existing rules preserved on modified lines
- DESIGN-02 selectors in login.css unchanged (verified by grep)
- passkeys.spec.ts passed or deferred with KC availability note
</success_criteria>

<output>
After completion, create .planning/phases/10-design-tokens-idp-theme/10-03-SUMMARY.md
</output>
