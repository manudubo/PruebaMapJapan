---
phase: 17
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/global-setup.ts
  - tests/e2e/session-management.spec.ts
autonomous: true
requirements:
  - SESSION-02
must_haves:
  truths:
    - "global-setup.ts kcLogin delegates to loginViaKcForm instead of having its own KC form navigation"
    - "global-setup.ts kcLoginNewUser delegates to loginViaKcForm instead of having its own KC form navigation"
    - "session-management.spec.ts loginViaBrowser function is deleted"
    - "session-management.spec.ts call sites use loginViaKcForm directly"
    - "All 4 SESSION-02 call sites import from kc-login-helper.ts — zero duplicate KC nav implementations remain"
  artifacts:
    - path: "tests/global-setup.ts"
      provides: "Delegating kcLogin/kcLoginNewUser"
      contains: "loginViaKcForm("
    - path: "tests/e2e/session-management.spec.ts"
      provides: "Refactored session spec using shared helper"
      contains: "loginViaKcForm("
  key_links:
    - from: "tests/global-setup.ts"
      to: "tests/e2e/fixtures/kc-login-helper.ts"
      via: "import { loginViaKcForm } from './e2e/fixtures/kc-login-helper'"
      pattern: "from './e2e/fixtures/kc-login-helper'"
    - from: "tests/e2e/session-management.spec.ts"
      to: "tests/e2e/fixtures/kc-login-helper.ts"
      via: "import { loginViaKcForm } from './fixtures/kc-login-helper'"
      pattern: "from './fixtures/kc-login-helper'"
---

<objective>
Wire the shared loginViaKcForm helper into the remaining two call sites: global-setup.ts (kcLogin,
kcLoginNewUser) and session-management.spec.ts (loginViaBrowser). Depends on Plan 01 which creates
kc-login-helper.ts.

After this plan, a KC flow change requires one fix — only kc-login-helper.ts contains the form navigation.

Output: global-setup.ts (kcLogin + kcLoginNewUser delegate to helper), session-management.spec.ts
(loginViaBrowser deleted, all call sites use loginViaKcForm).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v3.1-phases/17-otp-login-helper/17-CONTEXT.md
@.planning/milestones/v3.1-phases/17-otp-login-helper/17-PATTERNS.md
@.planning/milestones/v3.1-phases/17-otp-login-helper/17-01-SUMMARY.md

<interfaces>
From tests/e2e/fixtures/kc-login-helper.ts (created in Plan 01):
  export async function loginViaKcForm(page: Page, username: string, password: string): Promise<void>
  Import path from global-setup.ts:            './e2e/fixtures/kc-login-helper'
  Import path from session-management.spec.ts: './fixtures/kc-login-helper'

From tests/global-setup.ts (current structure — read before editing):
  kcLogin() function:
    - Keep: mkdirSync, launch, newContext, newPage (top of function)
    - REPLACE: entire KC nav block (page.goto through page.waitForURL /dashboard\.html/) with:
        await loginViaKcForm(page, process.env.E2E_TEST_USERNAME!, process.env.E2E_TEST_PASSWORD!);
    - Keep: page.reload(), waitForLoadState, storageState(), sessionStorage writes, browser.close()

  kcLoginNewUser() function:
    - Keep: mkdirSync, launch, newContext, newPage (top of function)
    - REPLACE: entire KC nav block with:
        await loginViaKcForm(page, process.env.E2E_NEW_USER_USERNAME!, process.env.E2E_NEW_USER_PASSWORD!);
    - Keep: page.reload(), waitForLoadState, storageState(), sessionStorage writes, browser.close()

From tests/e2e/session-management.spec.ts (current structure — read before editing):
  loginViaBrowser(page, TEST_USER, TEST_PASS): async function (lines 44-83) — DELETE ENTIRELY
    The closing assertion inside loginViaBrowser:
      await expect(page.locator('#new-trip-btn')).toBeVisible({ timeout: 20_000 })
    MUST be moved to each call site after the loginViaKcForm call.
  Call sites: appear multiple times in the file — find ALL by searching for "loginViaBrowser("
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor global-setup.ts to delegate to loginViaKcForm</name>
  <files>tests/global-setup.ts</files>
  <read_first>
    - tests/global-setup.ts (read full content before editing)
    - tests/e2e/fixtures/kc-login-helper.ts (verify export exists from Plan 01)
  </read_first>
  <action>
Read global-setup.ts in full. Then make these targeted changes:

A. Add import after the existing import block (before the first function definition):
```typescript
import { loginViaKcForm } from './e2e/fixtures/kc-login-helper';
```

B. In kcLogin():
  Identify the KC navigation block: starts at page.goto(`${FRONTEND_URL}/...dashboard.html`)
  and ends at the page.waitForURL(/dashboard\.html/) call.
  Delete that entire block. Replace with exactly:
```typescript
  await loginViaKcForm(page, process.env.E2E_TEST_USERNAME!, process.env.E2E_TEST_PASSWORD!);
```
  Keep everything above (mkdirSync, browser.launch, newContext, newPage) and below
  (page.reload, waitForLoadState, context.storageState, sessionStorage writes, browser.close) unchanged.

C. In kcLoginNewUser():
  Identify the KC navigation block: starts at page.goto and ends at page.waitForURL(/dashboard\.html/).
  Delete that entire block. Replace with exactly:
```typescript
  await loginViaKcForm(page, process.env.E2E_NEW_USER_USERNAME!, process.env.E2E_NEW_USER_PASSWORD!);
```
  Keep everything above and below unchanged.

After editing, verify:
  - The file has zero occurrences of locator('#auth-login-prompt-btn') (nav body deleted)
  - The file has zero occurrences of locator('#username') or usernameField (nav body deleted)
  - storageState appears at least twice (post-login capture preserved in both functions)
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "from './e2e/fixtures/kc-login-helper'" tests/global-setup.ts returns 1 match
    - rg "loginViaKcForm\(" tests/global-setup.ts returns exactly 2 matches
    - rg "locator\('#auth-login-prompt-btn'\)" tests/global-setup.ts returns no match
    - rg "usernameField" tests/global-setup.ts returns no match
    - rg "storageState" tests/global-setup.ts returns at least 2 matches
    - rg "browser\.close\(\)" tests/global-setup.ts returns at least 2 matches
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>global-setup.ts kcLogin and kcLoginNewUser each delegate KC form navigation to loginViaKcForm; post-login state capture preserved in both.</done>
</task>

<task type="auto">
  <name>Task 2: Remove loginViaBrowser from session-management.spec.ts and wire loginViaKcForm</name>
  <files>tests/e2e/session-management.spec.ts</files>
  <read_first>
    - tests/e2e/session-management.spec.ts (read full content before editing)
    - tests/e2e/fixtures/kc-login-helper.ts (verify export name)
  </read_first>
  <action>
Read session-management.spec.ts in full. Then make these changes:

A. Add import after the existing import block:
```typescript
import { loginViaKcForm } from './fixtures/kc-login-helper';
```

B. Delete the loginViaBrowser function entirely, including:
  - Any JSDoc comment above the function
  - The function declaration and its entire body (ending at the closing brace)
  Note: the last statement inside loginViaBrowser is:
    await expect(page.locator('#new-trip-btn')).toBeVisible({ timeout: 20_000 });
  This assertion must be PRESERVED at each call site (see step C).

C. Find every call to loginViaBrowser (search for "loginViaBrowser(") in the test bodies.
   For each call site, replace the single loginViaBrowser call with TWO lines:
```typescript
    await loginViaKcForm(page, TEST_USER, TEST_PASS);
    await expect(page.locator('#new-trip-btn')).toBeVisible({ timeout: 20_000 });
```
   Use the correct page variable for each call site (some tests use tab1, pageA, tabA, etc.).
   If a call site already has an expect(#new-trip-btn) assertion immediately following it, do NOT
   add a duplicate — just replace loginViaBrowser with loginViaKcForm on that line.

D. After editing, verify:
  - loginViaBrowser appears 0 times in the file
  - loginViaKcForm appears at least as many times as there were loginViaBrowser call sites
  - #new-trip-btn appears in the file (assertion preserved)
  - getByRole('link', ... 'try another way') appears 0 times (was inside deleted loginViaBrowser)
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "from './fixtures/kc-login-helper'" tests/e2e/session-management.spec.ts returns 1 match
    - rg "loginViaKcForm" tests/e2e/session-management.spec.ts returns at least 4 matches
    - rg "loginViaBrowser" tests/e2e/session-management.spec.ts returns no match
    - rg "new-trip-btn" tests/e2e/session-management.spec.ts returns at least 1 match
    - rg "getByRole.*try another way" tests/e2e/session-management.spec.ts returns no match
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>loginViaBrowser deleted; all call sites replaced with loginViaKcForm + #new-trip-btn assertion; typechecks clean.</done>
</task>

</tasks>

<verification>
Per-task: npm run typecheck exits 0.
Wave gate (full stack): cd tests && npx playwright test e2e/session-management.spec.ts --project=chromium
</verification>

<success_criteria>
1. global-setup.ts: imports loginViaKcForm; both kcLogin and kcLoginNewUser delegate KC nav; post-login state preserved
2. session-management.spec.ts: loginViaBrowser deleted; all call sites replaced with loginViaKcForm + #new-trip-btn assertion
3. All 4 SESSION-02 call sites (global-setup x2, session-management, otp test 4) import from kc-login-helper.ts
4. npm run typecheck exits 0
</success_criteria>
