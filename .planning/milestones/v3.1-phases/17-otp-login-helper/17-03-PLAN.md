---
phase: 17
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - tests/e2e/otp.spec.ts
autonomous: true
requirements:
  - OTP-02
must_haves:
  truths:
    - "otp.spec.ts test 2 error assertion matches the actual backend error string returned by auth.ts:153"
    - "otp.spec.ts test 2 still verifies that an expired OTP is rejected (status 400)"
  artifacts:
    - path: "tests/e2e/otp.spec.ts"
      provides: "Fixed error assertion in test 2"
      contains: "otp_not_found"
---

<objective>
Close the one remaining verification gap from Phase 17: test 2 ("expired OTP is rejected") asserts
`expect(errText).toMatch(/expir/)` but the backend returns `{ error: 'otp_not_found' }` on the
expired-code path (auth.ts:151–154). The regex /expir/ cannot match 'otp_not_found', so test 2
will fail against the real backend.

Fix: update line 81 to `expect(errText).toMatch(/otp_not_found/)`. This is a one-line correction
that brings the test assertion into alignment with the actual backend contract.

Root cause: `getLatestUnexpiredOtp()` returns null for expired OTPs, so the verify route cannot
distinguish "expired" from "never requested" — both paths return `{ error: 'otp_not_found' }`.
Fixing the backend to return a distinct `otp_expired` code is out of scope (D-01: backend is
correct; phase boundary: pure stabilization).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v3.1-phases/17-otp-login-helper/17-CONTEXT.md
@.planning/milestones/v3.1-phases/17-otp-login-helper/17-VERIFICATION.md

<interfaces>
backend/src/routes/auth.ts lines 151–154 (otp-verify route, expired path):
  const otp = await getLatestUnexpiredOtp(db, userId);
  if (!otp) {
    const response: ApiResponse<never> = { success: false, error: 'otp_not_found' };
    return c.json(response, 400);
  }

  -- getLatestUnexpiredOtp returns null for BOTH expired OTPs and never-requested OTPs.
  -- The verify route returns otp_not_found (status 400) for both cases.
  -- The string 'otp_not_found' does NOT contain 'expir'.
  -- Test 2 constructs errText via: JSON.stringify(body).toLowerCase()
  -- So errText for the expired path = '{"success":false,"error":"otp_not_found"}'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test 2 error assertion in otp.spec.ts</name>
  <files>tests/e2e/otp.spec.ts</files>
  <read_first>
    - tests/e2e/otp.spec.ts (read full content to confirm line 81 is still the assertion)
    - backend/src/routes/auth.ts lines 145–175 (confirm otp_not_found is still returned)
  </read_first>
  <action>
In tests/e2e/otp.spec.ts, find the line:
  expect(errText).toMatch(/expir/);

Replace it with:
  expect(errText).toMatch(/otp_not_found/);

This is the only change. Do NOT touch any other assertion or test in the file.

Context: this line is in test 2 ("expired OTP is rejected"), inside the block that calls
kcAdmin.expireOtpCodes and then submits the OTP code. The status assertion on the line above
(expect([400, 401]).toContain(verifyRes.status())) is correct and must remain unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "toMatch\(/otp_not_found/\)" tests/e2e/otp.spec.ts returns 1 match
    - rg "toMatch\(/expir/\)" tests/e2e/otp.spec.ts returns no match
    - rg "expect\(\[400, 401\]\)\.toContain\(verifyRes\.status\(\)\)" tests/e2e/otp.spec.ts returns 1 match (status assertion preserved)
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>otp.spec.ts test 2 error assertion updated to /otp_not_found/ — matches actual backend error string. Status rejection check (400/401) preserved.</done>
</task>

</tasks>

<verification>
Static: npm run typecheck exits 0 + rg "otp_not_found" tests/e2e/otp.spec.ts returns 1 match.
Live (human): npx playwright test tests/e2e/otp.spec.ts --project=chromium against the full stack (KC + backend + frontend). All 4 OTP tests must pass.
</verification>

<success_criteria>
1. tests/e2e/otp.spec.ts line 81: `expect(errText).toMatch(/otp_not_found/)` — no more /expir/ mismatch
2. Status assertion `expect([400, 401]).toContain(verifyRes.status())` preserved (test still verifies rejection)
3. npm run typecheck exits 0
4. No other assertions in otp.spec.ts changed
</success_criteria>
