---
phase: 18
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/kc-admin.ts
autonomous: true
requirements:
  - PASS-02
  - PASS-03
must_haves:
  truths:
    - "resetCredentials calls client.users.update after deleting credentials to clear required actions"
    - "only the webauthn-register-passwordless required action is removed — other actions are preserved"
    - "resetCredentials does not call clearRequiredActions (separate function) — the clear is inline"
  artifacts:
    - path: "tests/e2e/fixtures/kc-admin.ts"
      provides: "Updated resetCredentials that clears webauthn-register-passwordless required action"
      contains: "webauthn-register-passwordless"
---

<objective>
Fix `resetCredentials` in kc-admin.ts so that the passkey campaign required action
`webauthn-register-passwordless` is cleared in addition to deleting credential records.

Root cause: when `passkeys.spec.ts` beforeEach calls `kcAdmin.resetCredentials`, it deletes
WebAuthn credential records — but if the previous test left `webauthn-register-passwordless`
in the user's requiredActions, KC presents the passkey registration flow at the start of the
next test, hijacking the session before the test can reach its own setup steps.

Fix: after deleting credentials, re-fetch the user's requiredActions and update them with
the `webauthn-register-passwordless` entry removed. Only that specific action is cleared;
other required actions (e.g. VERIFY_EMAIL, UPDATE_PASSWORD) are preserved.

Output: kc-admin.ts resetCredentials (modified — one extra fetch + update after the loop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
tests/e2e/fixtures/kc-admin.ts — current resetCredentials (read the file before editing):

  export async function resetCredentials(username: string): Promise&lt;void&gt; {
    const client = await buildAdminClient();
    const [user] = await client.users.find({ username, exact: true });
    if (!user?.id) throw new Error(`User not found: ${username}`);
    const credentials = await client.users.getCredentials({ id: user.id });
    for (const cred of credentials) {
      if (cred.type === 'webauthn-passwordless' || cred.type === 'webauthn') {
        await client.users.deleteCredential({ id: user.id, credentialId: cred.id! });
      }
    }
  }

KC Admin client API (from @keycloak/keycloak-admin-client):
  client.users.findOne({ id: string }): Promise&lt;UserRepresentation | undefined&gt;
    UserRepresentation.requiredActions: string[] | undefined

  client.users.update({ id: string }, UserRepresentation): Promise&lt;void&gt;
    Merges the provided fields into the user record.

The required action string to remove: 'webauthn-register-passwordless'
(set by KC's passkey campaign flow when webauthn-register-passwordless AIA is triggered)
</interfaces>
</context>

<threat_model>
No new attack surface. resetCredentials is a test-only helper that runs in beforeEach against
the local dev KC instance using admin credentials from .env.test. Clearing an extra required
action does not weaken auth — it removes a KC-enforced registration prompt, which is intentional
for test isolation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update resetCredentials to clear webauthn-register-passwordless required action</name>
  <files>tests/e2e/fixtures/kc-admin.ts</files>
  <read_first>
    - tests/e2e/fixtures/kc-admin.ts (read full file before editing)
  </read_first>
  <action>
Find the current resetCredentials function:

  export async function resetCredentials(username: string): Promise&lt;void&gt; {
    const client = await buildAdminClient();
    const [user] = await client.users.find({ username, exact: true });
    if (!user?.id) throw new Error(`User not found: ${username}`);
    const credentials = await client.users.getCredentials({ id: user.id });
    for (const cred of credentials) {
      if (cred.type === 'webauthn-passwordless' || cred.type === 'webauthn') {
        await client.users.deleteCredential({ id: user.id, credentialId: cred.id! });
      }
    }
  }

Replace it with:

  export async function resetCredentials(username: string): Promise&lt;void&gt; {
    const client = await buildAdminClient();
    const [user] = await client.users.find({ username, exact: true });
    if (!user?.id) throw new Error(`User not found: ${username}`);
    const credentials = await client.users.getCredentials({ id: user.id });
    for (const cred of credentials) {
      if (cred.type === 'webauthn-passwordless' || cred.type === 'webauthn') {
        await client.users.deleteCredential({ id: user.id, credentialId: cred.id! });
      }
    }
    // Clear webauthn-register-passwordless required action so the passkey campaign flow
    // cannot hijack the next test after a credential reset (PASS-02)
    const fresh = await client.users.findOne({ id: user.id });
    const filteredActions = (fresh?.requiredActions ?? []).filter(
      (a) => a !== 'webauthn-register-passwordless'
    );
    await client.users.update({ id: user.id }, { requiredActions: filteredActions });
  }

No other changes to the file.
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "webauthn-register-passwordless" tests/e2e/fixtures/kc-admin.ts returns at least 1 match (the filter expression)
    - rg "client\.users\.update" tests/e2e/fixtures/kc-admin.ts returns at least 1 match inside resetCredentials
    - rg "client\.users\.findOne" tests/e2e/fixtures/kc-admin.ts returns at least 1 match inside resetCredentials
    - rg "filteredActions" tests/e2e/fixtures/kc-admin.ts returns at least 2 matches (declaration and usage)
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>resetCredentials: after deleting WebAuthn credentials, fetches fresh user, filters out webauthn-register-passwordless, updates requiredActions. Other required actions preserved.</done>
</task>

</tasks>

<verification>
Static: npm run typecheck exits 0.
Live (needs stack, after plan 01 is also complete): npx playwright test tests/e2e/passkeys.spec.ts --project=chromium-passkeys
All 3 passkey tests must pass (PASS-03).
</verification>

<success_criteria>
1. tests/e2e/fixtures/kc-admin.ts resetCredentials calls client.users.findOne then client.users.update after the credential-delete loop
2. The filter expression removes 'webauthn-register-passwordless' while preserving other required actions
3. npm run typecheck exits 0
4. (PASS-03 — requires plan 01 also complete) passkeys.spec.ts passes all 3 tests under chromium-passkeys
</success_criteria>
