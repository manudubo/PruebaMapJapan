---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/passkeys.spec.ts
autonomous: true
requirements:
  - PASS-01
must_haves:
  truths:
    - "WebAuthn.removeVirtualAuthenticator is called only in afterEach, not in any test body"
    - "afterEach catches errors so cleanup never throws even if the authenticator was already removed"
    - "register button locator includes #btn-add-passkey (matches the actual profile.html button id)"
    - "guard message locator includes [data-passkey-guard] (matches the guard button rendered by openDeleteConfirm)"
  artifacts:
    - path: "tests/e2e/passkeys.spec.ts"
      provides: "Passkeys spec with afterEach cleanup and corrected selectors"
      contains: "afterEach"
  key_links:
    - from: "tests/e2e/passkeys.spec.ts"
      to: "frontend/src/pages/profile.ts"
      via: "selector #btn-add-passkey matches btn-add-passkey in profile.html; [data-passkey-guard] matches guardBtn attribute in openDeleteConfirm"
      pattern: "btn-add-passkey|data-passkey-guard"
---

<objective>
Fix two root causes of passkeys.spec.ts failures in the spec itself (no production code changes):

1. (PASS-01) Virtual authenticator cleanup is in each test body — if a test fails mid-way,
   `WebAuthn.removeVirtualAuthenticator` never runs and the stale authenticator affects the
   next test. Fix: move cleanup to `test.afterEach` with error-safe try/catch.

2. (Selector alignment — required for PASS-03) The spec's selectors never matched the actual
   profile.html / profile.ts output:
   - Register button: `[data-action="register-passkey"], #register-passkey-btn, button:has-text("Register passkey")`
     → actual button is `#btn-add-passkey` with text "Add passkey". Add `#btn-add-passkey` as a
     fallback in the existing locator list (profile.html is unchanged).
   - Last-credential guard: `[data-testid="last-credential-error"], .last-credential-warning, .error-message, [role="alert"]`
     → actual guard marker is `[data-passkey-guard]` (attribute already on the guard button in
     profile.ts openDeleteConfirm). Add `[data-passkey-guard]` as a fallback (profile.ts is unchanged).

No production code is modified. This is spec-only stabilization, consistent with the v3.1 milestone scope.

Output: tests/e2e/passkeys.spec.ts (afterEach cleanup + corrected locator fallbacks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v3.1-phases/15-triage-config/15-TRIAGE.md

<interfaces>
tests/e2e/passkeys.spec.ts — current state (read full file before editing):
  - 3 tests inside test.describe('Passkey flows', ...)
  - test 1: creates cdp + authenticatorId; calls removeVirtualAuthenticator at end of body
  - test 2: creates cdpAuth + authId (main context) AND cdpClean + cleanAuthId (clean context);
            explicitly closes cleanContext mid-body; calls removeVirtualAuthenticator on authId at end
  - test 3: creates cdp + authenticatorId; calls removeVirtualAuthenticator at end of body

frontend/profile.html — actual button (no change needed):
  id="btn-add-passkey", text "Add passkey"

frontend/src/pages/profile.ts — openDeleteConfirm guard branch (no change needed):
  When credentialCount === 1, creates guardBtn with data-passkey-guard attribute set.
  This button IS visible when the guard is active — use [data-passkey-guard] in spec.
  Also: close() calls overlay.querySelector('[data-passkey-guard]')?.remove() so the
  attribute is gone after the modal closes — the selector is reliably state-specific.
</interfaces>
</context>

<threat_model>
No production code changes. All changes are to test files only. No new attack surface.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fix register button and guard message locators in passkeys.spec.ts</name>
  <files>tests/e2e/passkeys.spec.ts</files>
  <read_first>
    - tests/e2e/passkeys.spec.ts (read full file — locate all 3 registerBtn locators and the guardMsg locator)
    - frontend/src/pages/profile.ts (confirm data-passkey-guard attribute name in openDeleteConfirm)
    - frontend/profile.html (confirm id="btn-add-passkey" on the register button)
  </read_first>
  <action>
Make these two targeted selector changes:

CHANGE A — Register button locator (appears 3 times in the file, once per test):

Find every occurrence of:
  '[data-action="register-passkey"], #register-passkey-btn, button:has-text("Register passkey")'

Replace each with:
  '[data-action="register-passkey"], #register-passkey-btn, #btn-add-passkey, button:has-text("Add passkey"), button:has-text("Register passkey")'

There are exactly 3 occurrences (tests 1, 2, and 3). Replace all 3.

CHANGE B — Guard message locator (appears 1 time in test 3):

Find:
  '[data-testid="last-credential-error"], .last-credential-warning, .error-message, [role="alert"]'

Replace with:
  '[data-testid="last-credential-error"], .last-credential-warning, .error-message, [role="alert"], [data-passkey-guard]'

No other locator changes.
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "btn-add-passkey" tests/e2e/passkeys.spec.ts returns 3 matches (one per test)
    - rg "data-passkey-guard" tests/e2e/passkeys.spec.ts returns 1 match (guard message locator)
    - rg "Register passkey" tests/e2e/passkeys.spec.ts returns 3 matches (old fallback preserved)
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Register button locator now includes #btn-add-passkey; guard message locator includes [data-passkey-guard].</done>
</task>

<task type="auto">
  <name>Task 2: Move removeVirtualAuthenticator to afterEach (PASS-01)</name>
  <files>tests/e2e/passkeys.spec.ts</files>
  <read_first>
    - tests/e2e/passkeys.spec.ts (full file — critical to understand CDP session structure in all 3 tests before editing)
  </read_first>
  <action>
Read the full file. Make these changes in order:

STEP A — Add import for BrowserContext after the existing first import line:
  import type { BrowserContext } from '@playwright/test';

STEP B — Inside the test.describe('Passkey flows', () => { callback, BEFORE the existing
test.skip line, add the cleanup registry and afterEach hook:

  // Cleanup registry — afterEach drains unconditionally so a mid-test failure cannot leave
  // a stale virtual authenticator for subsequent tests (PASS-01)
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const cdpCleanups: Array&lt;{ cdp: any; authenticatorId: string; context?: BrowserContext }&gt; = [];

  test.afterEach(async () => {
    for (const { cdp, authenticatorId, context } of cdpCleanups) {
      try {
        await cdp.send('WebAuthn.removeVirtualAuthenticator', { authenticatorId });
      } catch { /* already removed or session closed */ }
      if (context) {
        try { await context.close(); } catch { /* already closed */ }
      }
    }
    cdpCleanups.length = 0;
  });

STEP C — Refactor test 1 ("register passkey via CDP Virtual Authenticator"):

After the `const { authenticatorId } = await cdp.send('WebAuthn.addVirtualAuthenticator', { ... });`
call (the full addVirtualAuthenticator block ends with its closing `});`), add on the next line:
  cdpCleanups.push({ cdp, authenticatorId });

Then remove this line from the END of test 1 (just before the closing `});` of the test):
  await cdp.send('WebAuthn.removeVirtualAuthenticator', { authenticatorId });

STEP D — Refactor test 2 ("login with passkey via KC login form"):

After `const { authenticatorId: authId } = await cdpAuth.send('WebAuthn.addVirtualAuthenticator', { ... });`
add on the next line:
  cdpCleanups.push({ cdp: cdpAuth, authenticatorId: authId });

After `const { authenticatorId: cleanAuthId } = await cdpClean.send('WebAuthn.addVirtualAuthenticator', { ... });`
add on the next line:
  cdpCleanups.push({ cdp: cdpClean, authenticatorId: cleanAuthId, context: cleanContext });

Then remove this line from the END of test 2 (just before the closing `});` of the test):
  await cdpAuth.send('WebAuthn.removeVirtualAuthenticator', { authenticatorId: authId });

IMPORTANT: Keep `await cleanContext.close();` in the test 2 body — explicit close for success path.
The afterEach entry for cleanContext provides the safety net for failure path.

STEP E — Refactor test 3 ("delete passkey is blocked when it is the last credential"):

After `const { authenticatorId } = await cdp.send('WebAuthn.addVirtualAuthenticator', { ... });`
add on the next line:
  cdpCleanups.push({ cdp, authenticatorId });

Then remove this line from the END of test 3 (just before the closing `});` of the test):
  await cdp.send('WebAuthn.removeVirtualAuthenticator', { authenticatorId });
  </action>
  <verify>
    <automated>cd C:/Users/manud/Documents/GitHub/PruebaMapJapan &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rg "afterEach" tests/e2e/passkeys.spec.ts returns at least 1 match
    - rg "cdpCleanups\.push" tests/e2e/passkeys.spec.ts returns exactly 3 matches (one per test)
    - rg "removeVirtualAuthenticator" tests/e2e/passkeys.spec.ts returns exactly 1 match (in afterEach only)
    - rg "import type \{ BrowserContext \}" tests/e2e/passkeys.spec.ts returns 1 match
    - rg "cleanContext\.close" tests/e2e/passkeys.spec.ts returns at least 1 match (test 2 explicit close preserved)
    - rg "cdpCleanups\.length = 0" tests/e2e/passkeys.spec.ts returns 1 match
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>afterEach drains cleanup registry; removeVirtualAuthenticator appears only in afterEach; 3 tests push to cdpCleanups; test 2 still closes cleanContext explicitly.</done>
</task>

</tasks>

<verification>
Per-task: npm run typecheck exits 0.
Live (needs stack): npx playwright test tests/e2e/passkeys.spec.ts --project=chromium-passkeys
</verification>

<success_criteria>
1. passkeys.spec.ts: register button locator includes #btn-add-passkey (3 occurrences)
2. passkeys.spec.ts: guard message locator includes [data-passkey-guard] (1 occurrence)
3. passkeys.spec.ts: afterEach drains cdpCleanups with try/catch; removeVirtualAuthenticator appears only in afterEach
4. npm run typecheck exits 0
5. No production files modified (profile.html, profile.ts unchanged)
</success_criteria>
