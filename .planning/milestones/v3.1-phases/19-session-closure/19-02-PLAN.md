---
phase: 19-session-closure
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md
autonomous: false
requirements: [SESSION-01, DOC-02]

must_haves:
  truths:
    - "session-management.spec.ts passes on chromium, firefox, webkit with loginViaKcForm on a live stack (SKIP_REAL_AUTH unset)"
    - "Full E2E suite with SKIP_REAL_AUTH unset produces 0 unexplained failures — every outcome is green, fixme (accepted), or has a documented resolution"
    - "19-CLOSURE.md exists and covers all 7 Phase-15-triage entries with resolution notes and final statuses from the D-05 run"
  artifacts:
    - path: ".planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md"
      provides: "v3.1 milestone closure document with per-spec status table and accepted deferrals"
      contains: "Per-Spec Status Table"
  key_links:
    - from: "D-05 full suite run output"
      to: "19-CLOSURE.md"
      via: "actual summary line (N passed, M skipped, 0 failed)"
      pattern: "passed.*skipped.*failed"
---

<objective>
Run the authoritative full E2E suite (D-05) and write the v3.1 milestone closure document (D-06). Before the suite run, confirm session-management.spec.ts passes individually (SESSION-01 live verification per D-01). After the full run, write 19-CLOSURE.md covering all 7 Phase-15-triage failures with their final resolution status.

Purpose: Close v3.1 E2E Stabilization. The full suite run is the milestone gate — zero unexplained failures with SKIP_REAL_AUTH unset.
Output: 19-CLOSURE.md committed; v3.1 milestone declared complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v3.1-phases/19-session-closure/19-CONTEXT.md
@.planning/milestones/v3.1-phases/19-session-closure/19-RESEARCH.md
@.planning/milestones/v3.1-phases/15-triage-config/15-TRIAGE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify live stack is running</name>
  <what-built>The full E2E suite (D-05) and the session-management live check (D-01) both require the local development stack: Keycloak (localhost:8080), backend API (localhost:8787), and frontend (localhost:5173). SKIP_REAL_AUTH must be unset — a run with SKIP_REAL_AUTH=1 would silently skip all real-auth specs and produce a false-green closure.</what-built>
  <how-to-verify>
    1. Confirm SKIP_REAL_AUTH is NOT set:
       `echo $SKIP_REAL_AUTH` (bash) or `$env:SKIP_REAL_AUTH` (PowerShell) → must be empty

    2. Keycloak: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080`
       → expected: 200 or 303

    3. Backend: `curl -s http://localhost:8787/api/health`
       → expected: JSON containing `"status":"ok"` or `"ok":true`

    4. Frontend: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/PruebaMapJapan/`
       → expected: 200

    If any service is not running, start it (from project root):
    ```
    cd keycloak && docker compose up -d   # wait ~15s for Keycloak realm import
    cd ..
    npm run dev:backend                   # in a separate terminal, stays running
    npm run dev:frontend                  # in another terminal, stays running
    ```
    Re-run health checks until all three pass.
  </how-to-verify>
  <resume-signal>Type "stack ready" when SKIP_REAL_AUTH is unset and all 3 health checks pass</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run session-management check (D-01) then full suite (D-05)</name>
  <files></files>
  <read_first>
    - tests/e2e/session-management.spec.ts — confirm loginViaKcForm import is present (line 22) and used in all 6 tests; this is the D-01 structural check before the live run
    - tests/e2e/fixtures/kc-login-helper.ts — confirm loginViaKcForm(page, username, password) signature exists
  </read_first>
  <action>
    CRITICAL: SKIP_REAL_AUTH must be unset before any test command. A run with SKIP_REAL_AUTH=1 would skip all real-auth specs, produce a false-green result, and an invalid closure document. If it is set, unset it: `unset SKIP_REAL_AUTH` (bash) or `Remove-Item Env:SKIP_REAL_AUTH` (PowerShell).

    All test commands run from the `tests/` directory.

    **Step 1: Delete stale auth files (Pitfall 4 — expired KC tokens cause silent auth redirects)**
    ```bash
    rm -f .auth/user.json .auth/new-user.json .auth/new-user-session.json
    ```
    On Windows PowerShell:
    ```powershell
    Remove-Item .auth/user.json, .auth/new-user.json, .auth/new-user-session.json -ErrorAction SilentlyContinue
    ```

    **Step 2: Verify session-management.spec.ts passes (D-01 / SESSION-01)**
    ```bash
    npx playwright test session-management.spec.ts --trace retain-on-failure
    ```
    If this command exits non-zero (unexpected failure), STOP. Do not run the full suite. Investigate the failure trace in test-results/ and resolve it before proceeding. If it passes (exit 0), continue.

    **Step 3: Run full suite (D-05)**
    ```bash
    npx playwright test --trace retain-on-failure --retries 1
    ```
    Capture the complete output. The final summary line format is: `N passed, M skipped, 0 failed`.

    **Step 4: Record the summary**
    Note the exact final summary line from the D-05 run output — it is the direct input to Task 3 for 19-CLOSURE.md. Note which tests are green vs. fixme/skipped.
  </action>
  <verify>
    <automated>
      cd tests &amp;&amp; npx playwright test session-management.spec.ts --trace retain-on-failure --reporter=list 2>&amp;1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - SKIP_REAL_AUTH is unset during the run (real-auth specs are NOT skipped — they must run and pass)
    - `npx playwright test session-management.spec.ts` exits 0 with all 6 tests passing (not skipped)
    - `npx playwright test --trace retain-on-failure --retries 1` completes and the final summary contains `0 failed`
    - The summary line contains actual numbers (e.g., "210 passed") — NOT a run where session-management shows as "skipped"
    - D-05 run output captured and available for Task 3
  </acceptance_criteria>
  <done>session-management 6 tests pass (not skipped); full suite exits with 0 unexplained failures; summary line with actual pass count captured</done>
</task>

<task type="auto">
  <name>Task 3: Write 19-CLOSURE.md (D-06)</name>
  <files>.planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md</files>
  <read_first>
    - .planning/milestones/v3.1-phases/15-triage-config/15-TRIAGE.md — authoritative failure table; every entry in the table must appear in the 19-CLOSURE.md per-spec status table
    - D-05 run output from Task 2 — exact summary line and per-spec results
  </read_first>
  <action>
    Create `.planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md` with the following structure (per D-06):

    **Section 1: Header**
    - Date: today's date
    - Suite run command: `npx playwright test --trace retain-on-failure --retries 1` (from tests/)
    - Result: exact summary line from D-05 run (e.g., "210 passed, 7 skipped, 0 failed") — use actual numbers from the run, not placeholders

    **Section 2: Per-Spec Status Table**
    Cover every row from 15-TRIAGE.md Failure Table that was Fail. Required entries (fill Final Status from actual D-05 run output):

    | Spec | Project(s) | Final Status | Resolution |
    |------|-----------|--------------|------------|
    | auth.spec.ts (real-session) | firefox, webkit | [from D-05] | D-03: added storageState to firefox/webkit projects in playwright.config.ts (plan 19-01) |
    | idp-theme.spec.ts | chromium | [from D-05] | Phase 16 THEME-01/02/03 (commit 04c6a04) |
    | new-user-trip-creation.spec.ts | webkit | FIXME (accepted) | D-04: test.fixme(webkit, ...) — environment constraint, not app bug |
    | otp.spec.ts | chromium, firefox, webkit | [from D-05] | Phase 17 OTP-01/02/03 (commits 75b873f, d042197) |
    | passkeys.spec.ts | chromium-passkeys | [from D-05] | Phase 18 PASS-01/02/03 (commit 86281e9) |
    | session-management.spec.ts | chromium, firefox, webkit | [from D-05] | Phase 17 + Phase 19 live run (loginViaKcForm wired) |
    | trip-edit-integration.spec.ts | chromium, firefox, webkit | FIXME (accepted) | D-02: test.fixme(true, ...) — backend integration not implemented in v3.1 |

    Replace [from D-05] with the actual status: GREEN if tests passed, FIXME (accepted) if marked fixme.

    **Section 3: Accepted Deferrals**
    | Spec | Condition | Rationale |
    |------|-----------|-----------|
    | trip-edit-integration.spec.ts | unconditional | trip-edit API integration not implemented in v3.1 — tests pre-written for future Phase 2 integration; backend endpoints not yet built |
    | new-user-trip-creation.spec.ts | webkit only | webkit handles KC redirect differently when building a fresh browser context from new-user.json; spec passes on chromium and firefox |

    **Section 4: v3.1 Milestone Declaration**
    Short paragraph stating: "v3.1 E2E Stabilization is complete. Phases 15–19 addressed all failures from the Phase 15 triage. Zero unexplained failures remain. [actual N from D-05] tests pass, [actual M from D-05] tests are accepted fixme with documented rationale."

    Use actual numbers from the D-05 run in the milestone declaration.
  </action>
  <verify>
    <automated>
      test -f .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md &amp;&amp; grep -c "| " .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md` exists
    - File contains the exact D-05 summary line (actual numbers — must NOT contain placeholder strings "X passed" or "N passed" with letter variables)
    - File contains a table row for each of the 7 Phase-15-triage failures (auth real-session, idp-theme, new-user webkit, otp, passkeys, session-management, trip-edit-integration)
    - File contains `trip-edit-integration.spec.ts` in the Accepted Deferrals section with "backend endpoints not yet built" rationale
    - File contains `new-user-trip-creation.spec.ts` in the Accepted Deferrals section with "webkit" rationale
    - `grep "0 failed" .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md` returns a match (the D-05 run had 0 unexplained failures)
  </acceptance_criteria>
  <done>19-CLOSURE.md written with actual D-05 results; all 7 triage entries have resolutions; accepted deferrals documented; v3.1 milestone declared complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan runs existing tests against a local development stack and writes one planning artifact. No production code touched, no external trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | N/A | test runner + local stack | accept | Local dev stack only; all services are localhost; no external exposure |
</threat_model>

<verification>
After all tasks:
- `npx playwright test session-management.spec.ts` exits 0 with tests passing (not skipped) — D-01 / SESSION-01 confirmed
- Full suite exits with 0 failed, SKIP_REAL_AUTH was unset — D-05 / DOC-02 confirmed
- `cat .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md` shows actual pass count, 7 spec entries, 2 accepted deferrals
- `grep "0 failed" .planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md` matches
</verification>

<success_criteria>
- session-management.spec.ts passes on live stack with SKIP_REAL_AUTH unset (SESSION-01)
- Full E2E suite with SKIP_REAL_AUTH unset: 0 unexplained failures (DOC-02)
- 19-CLOSURE.md exists with actual D-05 numbers, 7 Phase-15-triage entries, and 2 accepted deferrals
- v3.1 milestone declared complete in closure doc
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.1-phases/19-session-closure/19-02-SUMMARY.md`
</output>
