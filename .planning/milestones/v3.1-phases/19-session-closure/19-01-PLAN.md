---
phase: 19-session-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/playwright.config.ts
  - tests/e2e/trip-edit-integration.spec.ts
  - tests/e2e/new-user-trip-creation.spec.ts
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "playwright.config.ts firefox and webkit projects carry storageState: '.auth/user.json' (conditional on SKIP_REAL_AUTH), matching the chromium project"
    - "trip-edit-integration.spec.ts has test.fixme(true, ...) at file scope — all 5 tests report as fixme in --list output"
    - "new-user-trip-creation.spec.ts has test.fixme with webkit callback — webkit tests report as fixme in --list output"
  artifacts:
    - path: "tests/playwright.config.ts"
      provides: "Consistent storageState config across all 3 non-passkeys browser projects"
      contains: "storageState: '.auth/user.json'"
    - path: "tests/e2e/trip-edit-integration.spec.ts"
      provides: "Documented fixme for unimplemented backend integration"
      contains: "test.fixme(true, 'trip-edit API integration not implemented"
    - path: "tests/e2e/new-user-trip-creation.spec.ts"
      provides: "webkit-scoped fixme for environment constraint"
      contains: "browserName === 'webkit'"
  key_links:
    - from: "tests/playwright.config.ts firefox/webkit projects"
      to: ".auth/user.json"
      via: "storageState config option"
      pattern: "storageState.*\\.auth/user\\.json"
    - from: "tests/e2e/trip-edit-integration.spec.ts"
      to: "DOC-01"
      via: "test.fixme(true, ...) at file scope"
      pattern: "test\\.fixme\\(true"
---

<objective>
Apply the three pre-decided failure dispositions for Phase 19: (1) add storageState to firefox/webkit browser projects in playwright.config.ts so auth.spec.ts real-session tests have consistent cross-browser config (D-03); (2) mark trip-edit-integration.spec.ts with an unconditional test.fixme at file scope — all 5 tests have the same unimplemented-backend root cause (D-02); (3) mark new-user-trip-creation.spec.ts webkit tests with a browser-conditional test.fixme (D-04).

Purpose: Eliminate all remaining config-bug and known-environment-constraint failures so the Phase 19-02 full suite run produces zero unexplained failures.
Output: Three modified files committed. Annotations verifiable via `npx playwright test --list`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v3.1-phases/19-session-closure/19-CONTEXT.md
@.planning/milestones/v3.1-phases/19-session-closure/19-RESEARCH.md
@.planning/milestones/v3.1-phases/19-session-closure/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storageState to firefox/webkit browser projects (D-03)</name>
  <files>tests/playwright.config.ts</files>
  <read_first>
    - tests/playwright.config.ts — read the full file; chromium project (lines 21-28) is the exact pattern to replicate in firefox and webkit
  </read_first>
  <action>
    Edit tests/playwright.config.ts. Replace the firefox and webkit project entries (currently lines 29-38) with the following, mirroring the chromium project's storageState conditional spread (per D-03):

    ```typescript
    {
      name: 'firefox',
      use: {
        ...devices['Desktop Firefox'],
        ...(process.env.SKIP_REAL_AUTH ? {} : { storageState: '.auth/user.json' }),
      },
      testIgnore: ['**/passkeys.spec.ts'],
    },
    {
      name: 'webkit',
      use: {
        ...devices['Desktop Safari'],
        ...(process.env.SKIP_REAL_AUTH ? {} : { storageState: '.auth/user.json' }),
      },
      testIgnore: ['**/passkeys.spec.ts'],
    },
    ```

    The chromium-passkeys project (lines 39-44) is unchanged — leave it as-is.

    Safety note: tests/e2e/idp-theme.spec.ts has `test.use({ storageState: { cookies: [], origins: [] } })` at its describe scope unconditionally — this overrides the newly added project storageState on firefox/webkit after this change, preserving idp-theme behavior. No regression.
  </action>
  <verify>
    <automated>
      cd tests &amp;&amp; npx playwright test playwright.config.ts --list 2>&amp;1 | head -5 || npx playwright test --list 2>&amp;1 | grep -c "firefox\|webkit\|chromium" | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/playwright.config.ts firefox project `use` block contains the line: `...(process.env.SKIP_REAL_AUTH ? {} : { storageState: '.auth/user.json' }),`
    - tests/playwright.config.ts webkit project `use` block contains the same line
    - The chromium project entry is unchanged (storageState spread already present at line 25)
    - The chromium-passkeys project entry is unchanged
    - `grep -c "storageState" tests/playwright.config.ts` returns 3 (chromium, firefox, webkit — one each)
  </acceptance_criteria>
  <done>firefox and webkit projects have identical storageState config to chromium; config parses (playwright --list runs without error)</done>
</task>

<task type="auto">
  <name>Task 2: Add test.fixme annotations to trip-edit-integration and new-user-trip-creation (D-02, D-04)</name>
  <files>tests/e2e/trip-edit-integration.spec.ts, tests/e2e/new-user-trip-creation.spec.ts</files>
  <read_first>
    - tests/e2e/trip-edit-integration.spec.ts — note line 9 (test.describe.configure), line 12 (test.setTimeout); insertion point is immediately after line 12
    - tests/e2e/new-user-trip-creation.spec.ts — note lines 5-9 (describe.configure + test.use storageState block); insertion point is immediately after the closing `});` of test.use on line 9
  </read_first>
  <action>
    **File 1: tests/e2e/trip-edit-integration.spec.ts (D-02)**

    Insert the following line immediately after `test.setTimeout(90000);` (line 12) and before the `// sessionStorage replay` comment block:

    ```typescript
    test.fixme(true, 'trip-edit API integration not implemented in v3.1 — pre-written for future Phase 2 integration');
    ```

    Do NOT place it inside a test body — it must be at file scope to mark all 5 tests. The file has no wrapping test.describe; file-scope placement marks all tests.
    Do NOT use test.skip — DOC-01 requires test.fixme semantics (known broken, will fix vs. intentionally excluded).

    **File 2: tests/e2e/new-user-trip-creation.spec.ts (D-04)**

    Insert the following block immediately after the closing `});` of the `test.use({ storageState: ... })` block (after line 9) and before the `// sessionStorage replay` comment (line 11):

    ```typescript
    test.fixme(
      ({ browserName }) => browserName === 'webkit',
      'webkit handles KC redirect differently when building a fresh browser context from new-user.json — environment constraint',
    );
    ```

    The callback form uses Playwright fixture injection — it is more reliable than `process.env.WEBKIT` env-var parsing for browser-conditional marking. The existing `test.skip(!!process.env.SKIP_REAL_AUTH, ...)` inside the describe block at line 51 is orthogonal and must remain unchanged.
  </action>
  <verify>
    <automated>
      cd tests &amp;&amp; npx playwright test trip-edit-integration.spec.ts --list 2>&amp;1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/trip-edit-integration.spec.ts contains the exact string: `test.fixme(true, 'trip-edit API integration not implemented in v3.1`
    - The fixme call appears at file scope (before any `test(` declaration)
    - `npx playwright test trip-edit-integration.spec.ts --list` (from tests/) output shows all 5 tests (P2-V1 through P2-V5) with fixme annotation — zero tests listed as runnable
    - tests/e2e/new-user-trip-creation.spec.ts contains the exact string: `({ browserName }) => browserName === 'webkit'`
    - The webkit fixme call appears after the test.use storageState block and before the test.describe block
    - `npx playwright test new-user-trip-creation.spec.ts --project=webkit --list` (from tests/) shows the NU-01 test as fixme
    - `npx playwright test new-user-trip-creation.spec.ts --project=chromium --list` (from tests/) shows the NU-01 test as runnable (not fixme)
    - `npm run typecheck` exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>Both spec files annotated; trip-edit all 5 tests fixme; new-user webkit fixme, chromium/firefox runnable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan modifies test configuration and test annotation files only. No production code, no auth boundaries, no user data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | N/A | test layer | accept | All changes are in tests/; zero production attack surface |
</threat_model>

<verification>
After both tasks:
- `npx playwright test trip-edit-integration.spec.ts --list` shows 5 fixme tests, 0 runnable
- `npx playwright test new-user-trip-creation.spec.ts --project=webkit --list` shows 1 fixme test
- `npx playwright test new-user-trip-creation.spec.ts --project=chromium --list` shows 1 runnable test (not fixme)
- `grep -c "storageState" tests/playwright.config.ts` returns 3
- playwright.config.ts diff shows storageState spread added to firefox and webkit `use` blocks only
- Pre-commit gate: `npm run typecheck` exits 0 — no TypeScript errors introduced by any of the three file changes
</verification>

<success_criteria>
- playwright.config.ts: firefox and webkit projects carry `...(process.env.SKIP_REAL_AUTH ? {} : { storageState: '.auth/user.json' })` in their `use` blocks
- trip-edit-integration.spec.ts: `test.fixme(true, ...)` at file scope before any test declaration
- new-user-trip-creation.spec.ts: `test.fixme(({ browserName }) => browserName === 'webkit', ...)` after test.use block
- All three changes committed
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.1-phases/19-session-closure/19-01-SUMMARY.md`
</output>
