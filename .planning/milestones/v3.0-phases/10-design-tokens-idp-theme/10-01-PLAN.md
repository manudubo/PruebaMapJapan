---
phase: 10-design-tokens-idp-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/main.css
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-04]

must_haves:
  truths:
    - "Every var() call in main.css component rules references a --jp-* token"
    - "No hardcoded hex or rgba value appears in component rules (outside :root, [data-theme='dark'], prefers-contrast, and @media print blocks)"
    - "The 8 extended D-03 tokens exist in :root"
    - "The prefers-contrast @media block uses --jp-border and --jp-text-secondary"
    - "The existing anti-FOUC script in all 13 HTML files is untouched"
  artifacts:
    - path: "frontend/src/styles/main.css"
      provides: "Complete --jp-* token vocabulary + zero hardcoded component rule values"
      contains: "--jp-bg"
  key_links:
    - from: "main.css :root"
      to: "main.css component rules"
      via: "var(--jp-*)"
      pattern: "var\\(--jp-"
---

<objective>
Rename all CSS custom properties in frontend/src/styles/main.css from the legacy generic names to the --jp-* prefix (D-01), add 8 new semantic tokens to eliminate residual hardcoded rgba/hex in component rules (D-03 + RESEARCH audit extension), and fix all ~30 component-rule hardcoded values by replacing them with var(--jp-*) references.

DESIGN-02 and DESIGN-04 are already satisfied by existing code — this plan includes a regression-guard task (no file changes) to verify those remain intact.

Purpose: main.css is the single source of truth for the app's visual language. After this plan every component rule uses var(--jp-*) exclusively.
Output: main.css with renamed :root block, renamed [data-theme="dark"] block, renamed prefers-contrast block, 8 new D-03 tokens, all component-rule hardcoded values replaced with token references.
</objective>

<execution_context>
@C:/Users/manud/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/manud/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-design-tokens-idp-theme/10-CONTEXT.md
@.planning/phases/10-design-tokens-idp-theme/10-RESEARCH.md
@.planning/phases/10-design-tokens-idp-theme/10-PATTERNS.md
@.planning/phases/10-design-tokens-idp-theme/10-UI-SPEC.md

<interfaces>
<!-- Token name mappings — old → new. All fallback hex values in var() calls stay unchanged. -->

D-01 full mapping (apply in :root, [data-theme="dark"], prefers-contrast blocks AND every var() usage in component rules):
  --font-sans           → --jp-font
  --bg-primary          → --jp-bg
  --bg-secondary        → --jp-surface       (collapse --bg-glass into this same name)
  --bg-glass            → --jp-surface       (same target as --bg-secondary)
  --bg-glass-strong     → --jp-surface-raised
  --bg-glass-subtle     → --jp-surface-subtle
  --border-color        → --jp-border
  --border-strong       → --jp-border-strong
  --border-glass        → --jp-border-glass
  --text-primary        → --jp-text
  --text-secondary      → --jp-text-secondary
  --text-tertiary       → --jp-text-tertiary
  --accent              → --jp-accent
  --accent-hover        → --jp-accent-hover
  --accent-subtle       → --jp-accent-subtle
  --success             → --jp-success
  --success-subtle      → --jp-success-subtle
  --danger              → --jp-danger
  --danger-hover        → --jp-danger-hover
  --danger-subtle       → --jp-danger-subtle
  --shadow-sm           → --jp-shadow-sm
  --shadow-md           → --jp-shadow-md
  --shadow-lg           → --jp-shadow-lg
  --shadow-glass        → --jp-shadow-glass
  --shadow-glass-hover  → --jp-shadow-glass-hover
  --radius              → --jp-radius
  --blur                → --jp-blur
  --blur-strong         → --jp-blur-strong
  --gradient-glass      → --jp-gradient-glass
  --gradient-shine      → --jp-gradient-shine
  --hotel               → --jp-hotel
  --optional            → --jp-optional
  --directions          → --jp-directions
  --marker-1            → --jp-marker-1
  --marker-2            → --jp-marker-2
  --marker-3            → --jp-marker-3
  --marker-4            → --jp-marker-4
  --marker-5            → --jp-marker-5
  --marker-6            → --jp-marker-6
  --marker-7            → --jp-marker-7
  --marker-8            → --jp-marker-8

Note on --gradient-glass: its value is var(--bg-secondary). After rename the value becomes var(--jp-surface).
Note on --gradient-glass in dark block: same — value becomes var(--jp-surface).

D-03 + RESEARCH extended new tokens (append to :root after the existing token definitions):
  --jp-hotel-subtle:   rgba(255, 149, 0, 0.08)
  --jp-hotel-border:   rgba(255, 149, 0, 0.2)
  --jp-optional-subtle: rgba(175, 82, 222, 0.1)
  --jp-optional-bg:    rgba(175, 82, 222, 0.05)   ← DISTINCT from optional-subtle; 0.05 not 0.1
  --jp-white:          #fff
  --jp-overlay:        rgba(0, 0, 0, 0.5)
  --jp-accent-border:  rgba(0, 113, 227, 0.2)
  --jp-marker-dash:    rgba(255, 255, 255, 0.6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all token definitions (D-01)</name>
  <files>frontend/src/styles/main.css</files>
  <read_first>
    - frontend/src/styles/main.css (read the full file — especially lines 1–100 for :root and [data-theme="dark"] blocks, and lines 1872–1881 for the prefers-contrast block)
  </read_first>
  <action>
Apply the D-01 rename in ALL THREE token-definition blocks. Do not touch component rules yet (Task 2 handles those).

1. :root block (approx lines 7–66): rename every custom property declaration from old name to --jp-* name per the mapping above.
   - --font-sans → --jp-font
   - --bg-primary → --jp-bg
   - --bg-secondary → --jp-surface
   - --bg-glass → --jp-surface  (this becomes a duplicate of --bg-secondary in the block — REMOVE the duplicate; keep only one --jp-surface definition)
   - --bg-glass-strong → --jp-surface-raised
   - --bg-glass-subtle → --jp-surface-subtle
   - --border-color → --jp-border
   - --border-strong → --jp-border-strong
   - --border-glass → --jp-border-glass
   - --text-primary → --jp-text
   - --text-secondary → --jp-text-secondary
   - --text-tertiary → --jp-text-tertiary
   - --accent → --jp-accent
   - --accent-hover → --jp-accent-hover
   - --accent-subtle → --jp-accent-subtle
   - --success → --jp-success
   - --success-subtle → --jp-success-subtle
   - --danger → --jp-danger
   - --danger-hover → --jp-danger-hover
   - --danger-subtle → --jp-danger-subtle
   - --shadow-sm → --jp-shadow-sm
   - --shadow-md → --jp-shadow-md
   - --shadow-lg → --jp-shadow-lg
   - --shadow-glass → --jp-shadow-glass
   - --shadow-glass-hover → --jp-shadow-glass-hover
   - --radius → --jp-radius
   - --blur → --jp-blur
   - --blur-strong → --jp-blur-strong
   - --gradient-glass: the value is var(--bg-secondary) — rename to: --jp-gradient-glass: var(--jp-surface)
   - --gradient-shine → --jp-gradient-shine
   - --hotel → --jp-hotel
   - --optional → --jp-optional
   - --directions → --jp-directions
   - --marker-1 through --marker-8 → --jp-marker-1 through --jp-marker-8

2. [data-theme="dark"] block (approx lines 68–95): same rename on every property listed there. The value var(--bg-secondary) inside --gradient-glass becomes var(--jp-surface).

3. @media (prefers-contrast: high) block (approx lines 1872–1881): rename the two property names used in the override:
   - --border-color → --jp-border
   - --text-secondary → --jp-text-secondary
   The values (#000, #333, #fff, #ccc) are token DEFINITION values inside this block — do not remove them.

4. Append the 8 new D-03 tokens to the end of the :root block (before the closing }):
   --jp-hotel-subtle: rgba(255, 149, 0, 0.08);
   --jp-hotel-border: rgba(255, 149, 0, 0.2);
   --jp-optional-subtle: rgba(175, 82, 222, 0.1);
   --jp-optional-bg: rgba(175, 82, 222, 0.05);
   --jp-white: #fff;
   --jp-overlay: rgba(0, 0, 0, 0.5);
   --jp-accent-border: rgba(0, 113, 227, 0.2);
   --jp-marker-dash: rgba(255, 255, 255, 0.6);

DO NOT touch component rules in this task.
  </action>
  <verify>
    <automated>
      cd frontend && rg -o "^\s*--[a-z][a-z0-9-]*" src/styles/main.css | rg -v "^\s*--jp-" | head -20
    </automated>
    Expected: empty output. All token definition lines are now --jp-* prefixed. Any line returned indicates a missed rename.
  </verify>
  <acceptance_criteria>
    - `rg -o "^\s*--[a-z][a-z0-9-]*" frontend/src/styles/main.css | rg -v "^\s*--jp-"` returns empty output
    - `rg "--jp-hotel-subtle" frontend/src/styles/main.css` returns a definition line in :root
    - `rg "--jp-optional-bg" frontend/src/styles/main.css` returns a definition line (distinct from --jp-optional-subtle)
    - `rg "--jp-white" frontend/src/styles/main.css` returns a definition line in :root
    - `rg "prefers-contrast" frontend/src/styles/main.css -A 8` output contains --jp-border and --jp-text-secondary (not --border-color or --text-secondary)
  </acceptance_criteria>
  <done>:root, [data-theme="dark"], and prefers-contrast blocks contain only --jp-* property declarations. 8 new D-03 tokens exist at the bottom of :root. No old-name token definition remains.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded values in component rules (D-01 + D-03)</name>
  <files>frontend/src/styles/main.css</files>
  <read_first>
    - frontend/src/styles/main.css (read entire file — needs to be open so all var() references in component rules can be updated)
  </read_first>
  <action>
Two sub-tasks: (a) rename all var(--old-name) usages in component rules to var(--jp-new-name), and (b) replace all hardcoded hex/rgba values in component rules with the appropriate token.

**Sub-task A: Rename var() usages in component rules**

Replace every `var(--old-name` with `var(--jp-new-name` (using the full D-01 mapping) throughout all component rules (lines ~96 to end, excluding print blocks). Fallback values inside var() are unchanged.

For --bg-glass and --bg-secondary usages: both map to var(--jp-surface, ...).

**Sub-task B: Replace hardcoded hex/rgba in component rules**

Apply these exact fixes (line numbers are approximate — verify by selector name):

| Selector | Old value | New value |
|---|---|---|
| .city-marker color | #fff | var(--jp-white) |
| .btn-primary color | #fff | var(--jp-white) |
| .btn-danger:hover color | #fff | var(--jp-white) |
| .overlay background | rgba(0,0,0,0.5) | var(--jp-overlay) |
| .top-nav a.is-active border-color | rgba(0,113,227,0.2) | var(--jp-accent-border) |
| .day-btn.active color | #fff | var(--jp-white) |
| .day-btn.has-options:hover background | rgba(175,82,222,0.1) | var(--jp-optional-subtle) |
| .day-btn.has-options.active color | #fff | var(--jp-white) |
| .day-group-badge color | #fff | var(--jp-white) |
| .legend-item.is-optional background (0.05) | rgba(175,82,222,0.05) | var(--jp-optional-bg) |
| .legend-item.is-optional background (second instance) | rgba(175,82,222,0.05) | var(--jp-optional-bg) |
| .legend-marker color | #fff | var(--jp-white) |
| .legend-action-btn:hover color | #fff | var(--jp-white) |
| .legend-action-btn.directions:hover color | #fff | var(--jp-white) |
| .hotel-info background | rgba(255,149,0,0.08) | var(--jp-hotel-subtle) |
| .hotel-info border | rgba(255,149,0,0.2) | var(--jp-hotel-border) |
| .hotel-info .marker color | #fff | var(--jp-white) |
| .calendar-btn:hover color | #fff | var(--jp-white) |
| .leaflet-popup-content .optional-badge color | #fff | var(--jp-white) |
| .popup-link:hover color | #fff | var(--jp-white) |
| .popup-link.directions:hover color | #fff | var(--jp-white) |
| .numbered-marker color | #fff | var(--jp-white) |
| .numbered-marker.optional border | rgba(255,255,255,0.6) | var(--jp-marker-dash) |
| .hotel-marker color | #fff | var(--jp-white) |
| .route-number color | #fff | var(--jp-white) |
| .skip-link color: white | white (keyword) | var(--jp-white) |
| .widget-error color: #ff3b30 | #ff3b30 | var(--jp-danger) |
| [data-theme="dark"] .widget-error color: #ff6961 | #ff6961 | var(--jp-danger) |

EXCLUSIONS — do NOT touch these:
- @media print blocks (approx L1811–1831, L1911–1919): #ddd and #000 are intentional print neutrals, explicitly out of DESIGN-01 scope
- :root { } token definition block: hex/rgba values ARE the token values, must remain
- [data-theme="dark"] { } block: same
- @media (prefers-contrast: high) :root { } block: token override values, must remain
- HTML <head> inline <style> blocks: not in main.css
  </action>
  <verify>
    <automated>
      cd frontend && rg -o "var\(--[a-z][a-z0-9-]*" src/styles/main.css | rg -v "^var\(--jp-"
    </automated>
    Expected: empty output. Any remaining `var(--old-name)` indicates a missed rename.

    <automated>
      cd frontend && npm run typecheck && npm run test:run
    </automated>
    Expected: zero TypeScript errors, all unit tests pass.
  </verify>
  <acceptance_criteria>
    - `cd frontend && rg -o "var\(--[a-z][a-z0-9-]*" src/styles/main.css | rg -v "^var\(--jp-"` returns empty output
    - `rg "var\(--jp-white\)" frontend/src/styles/main.css` returns 15+ matches (all former #fff / color:white usages)
    - `rg "var\(--jp-overlay\)" frontend/src/styles/main.css` returns 1 match (.overlay selector)
    - `rg "var\(--jp-optional-bg\)" frontend/src/styles/main.css` returns 2 matches (.legend-item.is-optional)
    - `rg "var\(--jp-danger\)" frontend/src/styles/main.css` returns matches including .widget-error
    - `cd frontend && npm run typecheck` exits 0
    - `cd frontend && npm run test:run` exits 0
  </acceptance_criteria>
  <done>
    - rg for old var() names returns empty output
    - All ~30 component-rule hardcoded hex/rgba values replaced with var(--jp-*) references
    - npm run typecheck exits 0
    - npm run test:run exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression guard — DESIGN-02 and DESIGN-04 still intact</name>
  <files>(no files modified)</files>
  <read_first>
    - No file reads required — this task runs verification commands only.
    - DESIGN-02 (KC login: Inter font, border-radius 0, no logo) is already satisfied by login.css and tested by idp-theme.spec.ts. Do NOT modify login.css in this plan.
    - DESIGN-04 (anti-FOUC script) is already satisfied by all 13 HTML files. Do NOT modify any HTML file. The existing script is a superset of D-04 spec (it also handles prefers-color-scheme) — do NOT replace it with D-04's "no fallback" literal spec, as that would be a regression.
  </read_first>
  <action>
Run the following verification commands to confirm DESIGN-02 and DESIGN-04 are still intact after the main.css changes. Make no file modifications.

1. Verify all 13 HTML files still contain the anti-FOUC localStorage script. Run these checks from the repo root:
   rg -l "localStorage.getItem" frontend/index.html frontend/tokyo.html frontend/nagoya.html frontend/takayama.html frontend/kyoto.html frontend/osaka.html frontend/naoshima.html frontend/hakone.html frontend/tokyo2.html frontend/dashboard.html frontend/trip.html frontend/trip-edit.html frontend/profile.html
   Expected: 13 file paths returned.

2. Confirm frontend/src/styles/main.css builds cleanly:
   cd frontend && npm run build
  </action>
  <verify>
    <automated>
      rg -l "localStorage.getItem" frontend/index.html frontend/tokyo.html frontend/nagoya.html frontend/takayama.html frontend/kyoto.html frontend/osaka.html frontend/naoshima.html frontend/hakone.html frontend/tokyo2.html frontend/dashboard.html frontend/trip.html frontend/trip-edit.html frontend/profile.html
    </automated>
    Expected: 13 file paths returned (one per line).

    <automated>
      cd frontend && npm run build
    </automated>
    Expected: build succeeds with zero errors.
  </verify>
  <acceptance_criteria>
    - `rg -l "localStorage.getItem" frontend/index.html frontend/tokyo.html frontend/nagoya.html frontend/takayama.html frontend/kyoto.html frontend/osaka.html frontend/naoshima.html frontend/hakone.html frontend/tokyo2.html frontend/dashboard.html frontend/trip.html frontend/trip-edit.html frontend/profile.html` returns 13 paths
    - `cd frontend && npm run build` exits 0
    - No HTML file has a git diff (git diff --name-only returns nothing for any *.html file)
    - `rg "kc-logo|border-radius.*0.*important" keycloak/themes/japan-trip/login/resources/css/login.css` returns matches (login.css DESIGN-02 rules untouched)
  </acceptance_criteria>
  <done>
    All 13 HTML files retain the localStorage.getItem reference. Build succeeds. No HTML or login.css file was modified by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom property names | Token names are compile-time constants in CSS source — no user input flows into property names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | main.css token names | accept | Token names are compile-time constants defined by the developer; no user input path exists; no mitigation needed beyond static diff review |
| T-10-02 | Tampering | @media print hardcoded values | accept | Print styles use intentional neutral hex (#ddd, #000); excluded from DESIGN-01 scope per research; no runtime theme path |
</threat_model>

<verification>
Final phase-gate verification for this plan (run after all 3 tasks complete):

1. No old token names in main.css:
   cd frontend && rg -o "var\(--[a-z][a-z0-9-]*" src/styles/main.css | rg -v "^var\(--jp-"
   → empty output

2. All 8 new D-03 tokens exist in main.css :root:
   rg "--jp-hotel-subtle|--jp-hotel-border|--jp-optional-subtle|--jp-optional-bg|--jp-white|--jp-overlay|--jp-accent-border|--jp-marker-dash" frontend/src/styles/main.css
   → 8 matches (one definition each)

3. Build and typecheck:
   cd frontend && npm run typecheck && npm run build
   → exits 0

4. Unit tests:
   cd frontend && npm run test:run
   → all pass

5. prefers-contrast block uses new names:
   rg "prefers-contrast" frontend/src/styles/main.css -A 10
   → shows --jp-border and --jp-text-secondary (not --border-color or --text-secondary)
</verification>

<success_criteria>
- main.css :root, [data-theme="dark"], and prefers-contrast blocks contain only --jp-* property names
- All 8 extended D-03 tokens defined in :root
- Zero hardcoded hex/rgba values remain in component rules (all replaced with var(--jp-*))
- @media print blocks unchanged (intentional exclusion)
- npm run typecheck exits 0
- npm run test:run all pass
- npm run build exits 0
- All 13 HTML anti-FOUC scripts untouched
</success_criteria>

<output>
After completion, create .planning/phases/10-design-tokens-idp-theme/10-01-SUMMARY.md
</output>
