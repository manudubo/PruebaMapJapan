# Requirements Archive: v3.1 E2E Stabilization

**Status:** ✅ SHIPPED 2026-07-23 — all 17 requirements complete, 0 outstanding.

**Milestone goal:** Get the full Playwright E2E suite green. Pure stabilization — no new product features.

---

## Suite Setup & Config

- [x] **SETUP-01**: A fresh full-suite E2E triage run with `trace: 'retain-on-failure'` and `retries: 1` produces an authoritative, current failure list against `main` — *Complete, Phase 15*
- [x] **SETUP-02**: `passkeys.spec.ts` is scoped to Chromium-only projects in `playwright.config.ts` (`testIgnore` on `chromium`, `firefox`, `webkit` projects) so CDP-only tests do not spuriously fail on non-Chromium runners — *Complete, Phase 15*

## Passkeys

- [x] **PASS-01**: `WebAuthn.removeVirtualAuthenticator` is called in `test.afterEach` (not in the test body) so a mid-test failure does not leave a stale authenticator for subsequent tests — *Complete, Phase 18*
- [x] **PASS-02**: `kcAdmin.resetCredentials` clears `webauthn-register-passwordless` required actions in addition to credential records, so the passkey campaign flow cannot hijack the next test — *Complete, Phase 18*
- [x] **PASS-03**: `passkeys.spec.ts` passes reliably under the `chromium-passkeys` project after the above fixes (no unexplained residual failure) — *Complete, Phase 18. Live-reverified 2026-07-23: 3/3 passing*

## OTP

- [x] **OTP-01**: `fetchLatestOtp()` uses a polling loop with a timeout (not a single-shot GET) so SMTP delivery lag between `otp-request` 200 and Mailpit message delivery does not cause false failures — *Complete, Phase 17*
- [x] **OTP-02**: `otp.spec.ts` tests 1–3 drive browser-based login first and inject a Bearer JWT into `request` calls, matching the auth-gated route contract (`backend/src/routes/auth.ts:92`) — *Complete, Phase 17*
- [x] **OTP-03**: `otp.spec.ts` test 4 (OTP via KC form) uses stable selectors for the current KC browser-flow shape and passes reliably — *Complete, Phase 17*

## Public Sharing

- [x] **SHARE-01**: `public-sharing.spec.ts` has a deterministic data fixture (seed extension or `beforeAll`) that creates the required public and private trips — no hardcoded UUIDs or IDs that depend on pre-existing runtime state — *Complete, Phase 16*
- [x] **SHARE-02**: The stale negative assertion against `'Cargando viaje…'` (Spanish loading placeholder removed in Phase 5) is replaced with a locale-correct positive assertion or removed — *Complete, Phase 16*

## IDP Theme

- [x] **THEME-01**: `idp-theme.spec.ts` runs with an empty `storageState` so no inherited KC SSO session prevents the login page from rendering — *Complete, Phase 16*
- [x] **THEME-02**: `idp-theme.spec.ts` uses a valid PKCE S256 `code_challenge` in `LOGIN_URL` (not the current `aaa…` placeholder), so KC 26 does not reject the auth request before the login page renders — *Complete, Phase 16*
- [x] **THEME-03**: All DOM and CSS assertions in `idp-theme.spec.ts` match the current KC 26 login/account template structure (no references to removed or renamed elements) — *Complete, Phase 16*

## Session Management

- [x] **SESSION-01**: `session-management.spec.ts` `loginViaBrowser()` uses stable selectors matching the current KC browser-flow shape (including the "Try Another Way → Password" branch) — *Complete, Phase 19. Live-verified 2026-07-23 against a live Keycloak stack*
- [x] **SESSION-02**: A shared `loginViaKcForm(page, username, password)` fixture is extracted from the four independent KC form-navigation implementations (`global-setup.ts` ×2, `session-management.spec.ts`, `otp.spec.ts`), so a KC flow change requires one fix — *Complete, Phase 17*

## Documentation

- [x] **DOC-01**: Any spec that is genuinely environment-specific (e.g., requires prod Keycloak config unavailable locally) is marked `test.fixme(condition, reason)` with an explicit rationale — no silently skipped or unexplained failures — *Complete, Phase 19. 25 accepted deferrals, all with documented rationale*
- [x] **DOC-02**: v3.1 closes with zero unexplained test failures — every outcome is either green, root-caused and fixed, or explicitly documented as an accepted deferral — *Complete, Phase 19. Final full-suite run: 242 passed, 25 skipped, 0 failed — see `.planning/milestones/v3.1-phases/19-session-closure/19-CLOSURE.md`*

---

## Requirement Outcomes

All 17 requirements shipped as scoped — no requirements dropped or materially adjusted during the milestone. Two requirements (SESSION-01, DOC-02) required substantially more investigation than their original phase plan anticipated (dedicated KC test user, passkeyCampaign root-cause fix, a real app bug fix in `tripDetail.ts`) but landed within the same requirement scope.

## Future Requirements (deferred to v3.2 candidate)

- OTP brute-force lockout: add `client.attackDetection.del({ id: user.id })` to `beforeEach` in `otp.spec.ts` to prevent lockout state from prior runs affecting subsequent tests
- Real-auth E2E in CI: Keycloak in CI environment; `SKIP_REAL_AUTH` guard removed from pipeline
- Per-recipient Mailpit isolation: switch `fetchLatestOtp()` to `GET /api/v1/search?query=to:unique-user@local` so the serial test constraint can be relaxed if OTP coverage expands to multiple personas

## Out of Scope (held for this milestone)

- New product features — v3.1 was stabilization only
- Playwright version upgrade — stayed on `^1.60.0`
- Rewriting currently-passing specs — changes to green specs would have introduced new risk
- Mocking KC auth in real-auth specs — these specs exist to catch real KC-flow regressions

---

## Traceability (final)

| REQ-ID | Phase | Status |
|--------|-------|--------|
| SETUP-01 | Phase 15 | ✓ Complete |
| SETUP-02 | Phase 15 | ✓ Complete |
| PASS-01 | Phase 18 | ✓ Complete |
| PASS-02 | Phase 18 | ✓ Complete |
| PASS-03 | Phase 18 | ✓ Complete |
| OTP-01 | Phase 17 | ✓ Complete |
| OTP-02 | Phase 17 | ✓ Complete |
| OTP-03 | Phase 17 | ✓ Complete |
| SHARE-01 | Phase 16 | ✓ Complete |
| SHARE-02 | Phase 16 | ✓ Complete |
| THEME-01 | Phase 16 | ✓ Complete |
| THEME-02 | Phase 16 | ✓ Complete |
| THEME-03 | Phase 16 | ✓ Complete |
| SESSION-01 | Phase 19 | ✓ Complete |
| SESSION-02 | Phase 17 | ✓ Complete |
| DOC-01 | Phase 19 | ✓ Complete |
| DOC-02 | Phase 19 | ✓ Complete |
