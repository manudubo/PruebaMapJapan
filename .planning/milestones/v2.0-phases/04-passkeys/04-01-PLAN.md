---
phase: 04-passkeys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keycloak/docker-compose.yml
  - keycloak/Dockerfile
  - keycloak/realm-export.json
  - frontend/package.json
autonomous: true
requirements:
  - PASS-02

must_haves:
  truths:
    - "Keycloak container starts on image 26.6.1 with no admin env-var deprecation warnings"
    - "realm-export.json imports with webAuthnPolicyPasswordlessRpId set to localhost"
    - "frontend TypeScript compiles cleanly against keycloak-js 26.x"
  artifacts:
    - path: "keycloak/docker-compose.yml"
      provides: "KC 26.6.1 image tag + renamed bootstrap admin env vars"
      contains: "quay.io/keycloak/keycloak:26.6.1"
    - path: "keycloak/Dockerfile"
      provides: "KC 26.6.1 in both builder and runtime FROM lines"
      contains: "quay.io/keycloak/keycloak:26.6.1"
    - path: "keycloak/realm-export.json"
      provides: "webAuthnPolicyPasswordlessRpId set to localhost"
      contains: '"webAuthnPolicyPasswordlessRpId": "localhost"'
    - path: "frontend/package.json"
      provides: "keycloak-js pinned to ^26.0.0"
      contains: '"keycloak-js": "^26.0.0"'
  key_links:
    - from: "keycloak/docker-compose.yml"
      to: "quay.io/keycloak/keycloak:26.6.1"
      via: "image: field"
      pattern: "keycloak:26\\.6\\.1"
    - from: "keycloak/realm-export.json"
      to: "WebAuthn passwordless registration"
      via: "webAuthnPolicyPasswordlessRpId field at JSON top-level"
      pattern: "webAuthnPolicyPasswordlessRpId.*localhost"
---

<objective>
Upgrade Keycloak infrastructure from 25.0 to 26.6.1 and configure the WebAuthn Passwordless
RP ID so passkey registration will work on localhost.

Purpose: These are blocking prerequisites. The realm-export.json RP ID change (D-02) cannot
be applied retroactively once a user registers a passkey. The typecheck gate ensures keycloak-js
25→26 TypeScript surface changes are caught before any new code is written (D-01).

Output: Four modified config files; npm install run; typecheck passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-passkeys/04-CONTEXT.md
@.planning/phases/04-passkeys/04-RESEARCH.md
@.planning/phases/04-passkeys/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Keycloak config files to 26.6.1 and set WebAuthn RP ID</name>
  <files>keycloak/docker-compose.yml, keycloak/Dockerfile, keycloak/realm-export.json</files>

  <read_first>
    - keycloak/docker-compose.yml — current content (image tag line 21, env vars lines 23-24)
    - keycloak/Dockerfile — current content (both FROM lines at lines 1 and 5)
    - keycloak/realm-export.json — current content (line 42: webAuthnPolicyPasswordlessRpId is "")
  </read_first>

  <action>
    Make the following surgical edits:

    **keycloak/docker-compose.yml** (3 changes, per D-01 and RESEARCH §5.2):
    - Line 21: `image: quay.io/keycloak/keycloak:25.0` → `image: quay.io/keycloak/keycloak:26.6.1`
    - Line 23: `KEYCLOAK_ADMIN: admin` → `KC_BOOTSTRAP_ADMIN_USERNAME: admin`
    - Line 24: `KEYCLOAK_ADMIN_PASSWORD: admin` → `KC_BOOTSTRAP_ADMIN_PASSWORD: admin`

    **keycloak/Dockerfile** (2 changes, per D-01):
    - Line 1: `FROM quay.io/keycloak/keycloak:25.0 AS builder` → `FROM quay.io/keycloak/keycloak:26.6.1 AS builder`
    - Line 5: `FROM quay.io/keycloak/keycloak:25.0` → `FROM quay.io/keycloak/keycloak:26.6.1`
    NOTE: This Dockerfile is NOT used by docker-compose (which pulls the image directly). The
    change is correct for completeness but has no effect on local `docker compose up`.

    **keycloak/realm-export.json** (1 change, per D-02):
    - Line 42: `"webAuthnPolicyPasswordlessRpId": ""` → `"webAuthnPolicyPasswordlessRpId": "localhost"`
    NOTE: After this change, a full `docker compose down -v && docker compose up` is MANDATORY to
    force re-import. A plain restart silently skips import if the realm already exists. Document
    this prominently in the SUMMARY.
  </action>

  <verify>
    <automated>
      cd C:\Users\manud\Documents\GitHub\PruebaMapJapan && grep "keycloak:26.6.1" keycloak/docker-compose.yml keycloak/Dockerfile && grep "KC_BOOTSTRAP_ADMIN_USERNAME" keycloak/docker-compose.yml && grep "KC_BOOTSTRAP_ADMIN_PASSWORD" keycloak/docker-compose.yml && node -e "const r=require('./keycloak/realm-export.json'); const v=r.webAuthnPolicyPasswordlessRpId; console.log('RP ID:', v); process.exit(v==='localhost'?0:1);"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "keycloak:26.6.1" keycloak/docker-compose.yml` returns exactly one match
    - `grep "keycloak:26.6.1" keycloak/Dockerfile` returns exactly two matches (builder + runtime)
    - `grep "KC_BOOTSTRAP_ADMIN_USERNAME" keycloak/docker-compose.yml` returns one match
    - `grep "KC_BOOTSTRAP_ADMIN_PASSWORD" keycloak/docker-compose.yml` returns one match
    - `grep "KEYCLOAK_ADMIN:" keycloak/docker-compose.yml` returns zero matches (old var removed)
    - `node -e "const r=require('./keycloak/realm-export.json'); process.exit(r.webAuthnPolicyPasswordlessRpId==='localhost'?0:1);"` exits 0
  </acceptance_criteria>

  <done>
    All three keycloak/* files updated. docker-compose.yml uses KC 26.6.1 image and new admin env
    var names. Dockerfile both FROM lines point to 26.6.1. realm-export.json has
    webAuthnPolicyPasswordlessRpId set to "localhost". Verification commands all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump keycloak-js to ^26.0.0, install, and run typecheck gate</name>
  <files>frontend/package.json</files>

  <read_first>
    - frontend/package.json — current content (line 25: "keycloak-js": "^25.0.0")
  </read_first>

  <action>
    Edit frontend/package.json:
    - Line 25: `"keycloak-js": "^25.0.0"` → `"keycloak-js": "^26.0.0"`

    Then from the frontend/ directory, run:
    1. `npm install` — resolves to keycloak-js 26.2.4 (latest 26.x per independent release cycle)
    2. `npm run typecheck` — MUST exit 0 before proceeding

    If typecheck fails with errors on `Keycloak.KeycloakTokenParsed` namespace access or import
    shape (per RESEARCH §2.7 and Risk 4): read the exact error, check frontend/src/auth/keycloak.ts
    for the affected import/type usage, and fix it. The most common fix is adjusting the namespace
    access from `Keycloak.KeycloakTokenParsed` to `KeycloakTokenParsed` (named import) if the
    namespace was flattened in 26.x.

    If typecheck passes on the first run: no code changes needed beyond the version bump.
  </action>

  <verify>
    <automated>
      cd C:\Users\manud\Documents\GitHub\PruebaMapJapan\frontend && npm run typecheck
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep '"keycloak-js"' frontend/package.json` shows `"keycloak-js": "^26.0.0"`
    - `grep '"keycloak-js"' frontend/package-lock.json` (or `node_modules/keycloak-js/package.json`)
      shows a resolved version matching `26.2.x`
    - `cd frontend && npm run typecheck` exits 0 with no errors
    - No `error TS2307: Cannot find module 'keycloak-js'` in output
    - No `error TS2339: Property 'KeycloakTokenParsed' does not exist` in output
  </acceptance_criteria>

  <done>
    frontend/package.json updated to ^26.0.0. npm install completed with keycloak-js 26.2.4
    installed. npm run typecheck exits 0. TypeScript compilation is clean — safe to write new
    profile.ts code in Plan 04-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host → Keycloak container | Admin credentials passed via environment variables |
| Browser → Keycloak | WebAuthn RP ID must match the origin; misconfiguration allows credential confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | webAuthnPolicyPasswordlessRpId in realm-export.json | mitigate | Set to "localhost" (exact match, not wildcard); browser WebAuthn API enforces effective domain check per W3C spec |
| T-04-02 | Elevation of Privilege | KC_BOOTSTRAP_ADMIN_USERNAME / KC_BOOTSTRAP_ADMIN_PASSWORD in docker-compose.yml | accept | Default "admin/admin" is local dev only; not committed with real secrets; docker-compose.yml is .gitignore-exempt for this project |
| T-04-03 | Tampering | realm-export.json import skipped on container restart | mitigate | Plan documents "docker compose down -v" requirement; SUMMARY warns executor; PATTERNS.md §Critical Notes item 2 confirms this risk |
</threat_model>

<verification>
After both tasks complete:

1. Config correctness:
   ```bash
   grep "keycloak:26.6.1" keycloak/docker-compose.yml keycloak/Dockerfile
   grep "KC_BOOTSTRAP_ADMIN" keycloak/docker-compose.yml
   node -e "const r=require('./keycloak/realm-export.json'); console.assert(r.webAuthnPolicyPasswordlessRpId==='localhost')"
   ```

2. TypeScript gate:
   ```bash
   cd frontend && npm run typecheck
   ```

3. PASS-02 verification (config check, no Keycloak needed):
   ```bash
   node -e "const r=require('./keycloak/realm-export.json'); const v=r.webAuthnPolicyPasswordlessRpId; console.log('RP ID:', v); process.exit(v==='localhost'?0:1);"
   ```

Manual verification note (required after Plan 04-02 is also complete):
- Run `docker compose down -v && docker compose up -d` from the keycloak/ directory to force
  realm re-import with the new RP ID. Plain restart does NOT apply the change.
- After container start, verify in Keycloak admin UI (http://localhost:8080/admin):
  japan-trip realm → Authentication → Required Actions → confirm "Webauthn Register Passwordless"
  is enabled. If not, enable it manually — it cannot be set via realm-export.json import.
</verification>

<success_criteria>
- keycloak/docker-compose.yml: image tag = 26.6.1, no KEYCLOAK_ADMIN* vars, KC_BOOTSTRAP_ADMIN_* vars present
- keycloak/Dockerfile: both FROM lines = 26.6.1
- keycloak/realm-export.json: webAuthnPolicyPasswordlessRpId = "localhost"
- frontend/package.json: keycloak-js = "^26.0.0"
- `cd frontend && npm run typecheck` exits 0
- All automated verify commands in both tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-passkeys/04-01-SUMMARY.md` using the summary template.

Include in SUMMARY:
- Which keycloak-js TypeScript issues (if any) were found and fixed during typecheck
- Confirmation that `docker compose down -v` is required before testing passkeys (not just restart)
- Note that Dockerfile change has no effect on local dev (docker-compose pulls image directly)
- Manual checkpoint: "Webauthn Register Passwordless" required action must be verified in KC admin UI
</output>
