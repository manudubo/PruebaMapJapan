---
phase: 05-internationalization-translate-all-user-facing-ui-strings-ht
plan: 10
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/modules/widgets.ts
  - frontend/src/modules/search.ts
autonomous: true
requirements: [I18N-TS, I18N-LOCALE]
must_haves:
  truths:
    - "widgets.ts WEATHER_CONDITIONS dictionary has English values (Clear, Cloudy, Rain, etc.)"
    - "widgets.ts section aria-label reads 'Local information for {city}'"
    - "widgets.ts h4 headings read 'Weather & Forecast', 'News', 'Events'"
    - "widgets.ts date formatting uses en-US locale"
    - "search.ts hotel subtitles read 'Hotel in {city}' (not 'Hotel en')"
    - "search.ts date formatting uses en-US locale"
    - "Both files have zero Spanish accent characters"
  artifacts:
    - path: "frontend/src/modules/widgets.ts"
      provides: "Translated widgets module with English weather conditions and en-US locale"
    - path: "frontend/src/modules/search.ts"
      provides: "Translated search module with English subtitles and en-US locale"
  key_links:
    - from: "widgets.ts:93"
      to: "weather forecast date display"
      via: "toLocaleDateString('es-ES')"
      pattern: "es-ES"
    - from: "search.ts:106,158"
      to: "search result date subtitles"
      via: "toLocaleDateString('es-ES')"
      pattern: "es-ES"
---

<objective>
Translate all Spanish strings in widgets.ts and search.ts — including the WEATHER_CONDITIONS dictionary, section headings, aria-labels, empty/error states, and locale calls.

Purpose: widgets.ts drives the local-info panel (weather, news, events) on city pages. search.ts drives the search index subtitles and date labels. Both contain es-ES locale calls invisible to accent-char grep.
Output: Both modules with English strings, English weather condition names, and en-US locale throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate widgets.ts</name>
  <files>frontend/src/modules/widgets.ts</files>
  <read_first>
    - frontend/src/modules/widgets.ts (read full file before editing)
    - 05-PATTERNS.md "Wave 7: frontend/src/modules/widgets.ts" section (lines 873–932)
  </read_first>
  <action>
Apply the following exact string replacements in frontend/src/modules/widgets.ts:

Line 33: section.setAttribute('aria-label', `Información local de ${cityName}`)
        → section.setAttribute('aria-label', `Local information for ${cityName}`)

Line 34: section.innerHTML = `<h3 class="widgets-title">Información Local: ${cityName}</h3>...`
        → change "Información Local: ${cityName}" → "Local Information: ${cityName}"
        (leave the rest of the innerHTML template intact)

Lines 38-47 (h4 headings inside innerHTML):
  <h4 id="weather-title">Clima & Pronóstico</h4> → <h4 id="weather-title">Weather & Forecast</h4>
  <h4 id="news-title">Noticias</h4> → <h4 id="news-title">News</h4>
  <h4 id="events-title">Eventos</h4> → <h4 id="events-title">Events</h4>

Lines 39-47 (sr-only loading strings inside innerHTML):
  <span class="sr-only">Cargando clima...</span> → <span class="sr-only">Loading weather...</span>
  <span class="sr-only">Cargando noticias...</span> → <span class="sr-only">Loading news...</span>
  <span class="sr-only">Cargando eventos...</span> → <span class="sr-only">Loading events...</span>

Line 93 (locale call in renderWeather()):
  toLocaleDateString('es-ES', { weekday: 'short' })
  → toLocaleDateString('en-US', { weekday: 'short' })

Lines 103-104 (aria-labels in renderWeather()):
  aria-label="Temperatura actual" → aria-label="Current temperature"
  aria-label="Pronóstico de 4 días" → aria-label="4-day forecast"

Lines 109-111 (WEATHER_CONDITIONS dictionary — replace all 9 values):
  0: 'Despejado' → 0: 'Clear'
  1: 'Poco nuboso' → 1: 'Mostly clear'
  2: 'Parcial nublado' → 2: 'Partly cloudy'
  3: 'Nublado' → 3: 'Cloudy'
  45: 'Niebla' → 45: 'Fog'
  51: 'Llovizna' → 51: 'Drizzle'
  61: 'Lluvia' → 61: 'Rain'
  71: 'Nieve' → 71: 'Snow'
  95: 'Tormenta' → 95: 'Thunderstorm'

Line 115 (fallback):
  'Variable' → 'Variable'  (unchanged — already works in English)

Line 197 (calendar button title/aria-label template):
  title="Agregar al calendario" → title="Add to calendar"
  aria-label="Agregar ${title} al calendario" → aria-label="Add ${title} to calendar"

Line 207 (empty state):
  `No se encontraron ${'noticias'|'eventos'} recientes.`
  → `No recent ${'news'|'events'} found.`
  (translate the ternary values: 'noticias' → 'news', 'eventos' → 'events')

Line 162 (error fallback):
  renderError(container, 'Recarga para ver contenido')
  → renderError(container, 'Reload to view content')

After editing, verify with:
  rg "[áéíóúñ¿¡]" frontend/src/modules/widgets.ts
  rg "es-ES" frontend/src/modules/widgets.ts
  </action>
  <verify>
    <automated>rg "[áéíóúñ¿¡]" frontend/src/modules/widgets.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg "[áéíóúñ¿¡]" frontend/src/modules/widgets.ts returns zero matches
    - rg "es-ES" frontend/src/modules/widgets.ts returns zero matches
  </acceptance_criteria>
  <done>widgets.ts has zero Spanish accent characters, WEATHER_CONDITIONS dictionary is English, locale is en-US</done>
</task>

<task type="auto">
  <name>Task 2: Translate search.ts</name>
  <files>frontend/src/modules/search.ts</files>
  <read_first>
    - frontend/src/modules/search.ts (read full file before editing)
    - 05-PATTERNS.md "Wave 7: frontend/src/modules/search.ts" section (lines 935–961)
  </read_first>
  <action>
Apply the following exact string replacements in frontend/src/modules/search.ts:

Line 49 (hotel subtitle in buildSearchIndex()):
  subtitle: `Hotel en ${cityData.name}`,
  → subtitle: `Hotel in ${cityData.name}`,

Line 106 (locale call — API trip destination subtitle):
  toLocaleDateString('es-ES', { day: 'numeric', month: 'short' })
  → toLocaleDateString('en-US', { day: 'numeric', month: 'short' })

Lines 117-118 (hotel subtitle in extendSearchIndexWithApiTrip):
  subtitle: `Hotel en ${dest.city_name} · ${trip.name}`,
  → subtitle: `Hotel in ${dest.city_name} · ${trip.name}`,

Lines 158-162 (formatDateLabel() locale call):
  toLocaleDateString('es-ES', { weekday: 'long', day: 'numeric', month: 'long' })
  → toLocaleDateString('en-US', { weekday: 'long', day: 'numeric', month: 'long' })

After editing, verify:
  rg "[áéíóúñ¿¡]" frontend/src/modules/search.ts
  rg "es-ES" frontend/src/modules/search.ts
  </action>
  <verify>
    <automated>rg "[áéíóúñ¿¡]" frontend/src/modules/search.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg "[áéíóúñ¿¡]" frontend/src/modules/search.ts returns zero matches
    - rg "es-ES" frontend/src/modules/search.ts returns zero matches
  </acceptance_criteria>
  <done>search.ts has zero Spanish accent characters, hotel subtitles use 'Hotel in', both locale calls use en-US</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | String-only TS edits — no auth or security changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | widgets.ts / search.ts strings | accept | Text-only changes; weather API calls and search indexing logic unchanged |
</threat_model>

<verification>
- `rg "[áéíóúñ¿¡]" frontend/src/modules/widgets.ts frontend/src/modules/search.ts` returns zero
- `rg "es-ES" frontend/src/modules/widgets.ts frontend/src/modules/search.ts` returns zero
</verification>

<success_criteria>
- widgets.ts: zero Spanish accent characters, WEATHER_CONDITIONS all English, locale en-US
- search.ts: zero Spanish accent characters, 'Hotel in' subtitles, locale en-US
</success_criteria>

<output>
After completion, create `.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-10-SUMMARY.md`
</output>
