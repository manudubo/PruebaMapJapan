---
phase: 05-internationalization-translate-all-user-facing-ui-strings-ht
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/pages/profile.ts
  - uat-passkeys.spec.ts
autonomous: true
requirements: [I18N-TS, I18N-LOCALE, I18N-PASSKEY]
must_haves:
  truths:
    - "profile.ts empty state reads 'You don't have any passkeys registered yet.'"
    - "profile.ts locale call uses 'en-US' not 'es-ES'"
    - "profile.ts delete modal reads 'Delete passkey?' with 'Delete' / 'Cancel' / 'Deleting…' buttons"
    - "uat-passkeys.spec.ts assertion uses 'You don't have any passkeys' (English substring)"
    - "Playwright UAT suite passes after both files are updated"
  artifacts:
    - path: "frontend/src/pages/profile.ts"
      provides: "Translated profile page module"
    - path: "uat-passkeys.spec.ts"
      provides: "Updated E2E spec matching translated profile.ts string"
  key_links:
    - from: "profile.ts:78"
      to: "uat-passkeys.spec.ts:108"
      via: "toContain() substring assertion"
      pattern: "No tenés passkeys"
---

<objective>
Translate all Spanish strings in profile.ts AND update the coupled uat-passkeys.spec.ts assertion in the same commit. These two files must never diverge — if profile.ts is translated without the spec update, the Playwright UAT suite will fail immediately.

Purpose: profile.ts drives the passkey management UI (register, list, delete). uat-passkeys.spec.ts directly asserts a substring from profile.ts line 78 — breaking that coupling breaks CI.
Output: profile.ts with English strings and en-US locale; uat-passkeys.spec.ts with updated toContain() assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate profile.ts and update uat-passkeys.spec.ts</name>
  <files>frontend/src/pages/profile.ts, uat-passkeys.spec.ts</files>
  <read_first>
    - frontend/src/pages/profile.ts (read full file before editing)
    - uat-passkeys.spec.ts (read full file before editing — check lines 105–115 and 180–195 for Spanish assertions)
    - 05-PATTERNS.md "Wave 3: frontend/src/pages/profile.ts" section (lines 381–424)
    - 05-PATTERNS.md "Wave 3 (same task): uat-passkeys.spec.ts" section (lines 440–454)
  </read_first>
  <action>
Apply the following exact string replacements in frontend/src/pages/profile.ts:

Line 78 (empty passkey list innerHTML):
  'No tenés passkeys registrados todavía.'
  → 'You don\'t have any passkeys registered yet.'

Lines 86-89 (locale call in map()):
  toLocaleDateString('es-ES', { day: 'numeric', month: 'short', year: 'numeric' })
  → toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' })

Line 96 (passkey meta span):
  `<span class="passkey-meta">Registrado: ${created}</span>`
  → `<span class="passkey-meta">Registered: ${created}</span>`

Line 99 (delete button text):
  Eliminar
  → Delete

Line 113 (error state innerHTML):
  'No se pudo cargar la lista de passkeys.'
  → 'Could not load passkey list.'

Line 127 (error in registerPasskey()):
  'Error al iniciar el registro de passkey.'
  → 'Error starting passkey registration.'

Lines 160-165 (modal innerHTML in buildDeleteModal()):
  <h2>¿Eliminar passkey?</h2>
  → <h2>Delete passkey?</h2>
  
  <p>Esta acción no se puede deshacer.</p>
  → <p>This action cannot be undone.</p>
  
  <button class="btn btn-secondary" id="passkey-delete-cancel">Cancelar</button>
  → <button class="btn btn-secondary" id="passkey-delete-cancel">Cancel</button>
  
  <button class="btn btn-danger" id="passkey-delete-confirm">Eliminar</button>
  → <button class="btn btn-danger" id="passkey-delete-confirm">Delete</button>

Line 199 (in-progress state):
  freshConfirm.textContent = 'Eliminando…'
  → freshConfirm.textContent = 'Deleting…'

Line 205 (restore after error):
  freshConfirm.textContent = 'Eliminar'
  → freshConfirm.textContent = 'Delete'

Apply the following exact string replacement in uat-passkeys.spec.ts:

Line 108:
  expect(initialText).toContain('No tenés passkeys')
  → expect(initialText).toContain("You don't have any passkeys")

CRITICAL: After editing uat-passkeys.spec.ts, grep lines 185 and 189 (and the surrounding ±5 lines) for any additional Spanish toContain() assertions and translate them to English. Do not leave any Spanish assertion strings.

Both files must be saved in the same commit — do not commit profile.ts before uat-passkeys.spec.ts.
  </action>
  <verify>
    <automated>rg "[áéíóúñ¿¡]" frontend/src/pages/profile.ts uat-passkeys.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg "[áéíóúñ¿¡]" frontend/src/pages/profile.ts returns zero matches
    - rg "[áéíóúñ¿¡]" uat-passkeys.spec.ts returns zero matches
    - rg "es-ES" frontend/src/pages/profile.ts returns zero matches
    - npx playwright test uat-passkeys.spec.ts passes
  </acceptance_criteria>
  <done>profile.ts has English strings and en-US locale; uat-passkeys.spec.ts assertion matches the new English string; Playwright UAT suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | String-only TS edits — no auth or security changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | profile.ts passkey strings | accept | Text-only changes; passkey logic unchanged; Playwright spec validates runtime behavior |
</threat_model>

<verification>
After task completes:
- `rg "[áéíóúñ¿¡]" frontend/src/pages/profile.ts uat-passkeys.spec.ts` returns zero
- `rg "es-ES" frontend/src/pages/profile.ts` returns zero
- `npx playwright test uat-passkeys.spec.ts` passes
</verification>

<success_criteria>
- profile.ts: zero Spanish accent characters, 'en-US' locale, English modal and button strings
- uat-passkeys.spec.ts: toContain assertion updated to "You don't have any passkeys"
- Playwright UAT spec passes without modification to test logic
</success_criteria>

<output>
After completion, create `.planning/phases/05-internationalization-translate-all-user-facing-ui-strings-ht/05-05-SUMMARY.md`
</output>
